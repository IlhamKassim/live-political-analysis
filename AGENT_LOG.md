# AGENT_LOG — MyPolitik (VPS night shift)

Append-only handoff log. Newest at the bottom.

2026-06-27 16:03 UTC · Codex · files: scripts/validate.sh, NIGHT_QUEUE.md, AGENT_LOG.md · commit: BLOCKED (.git/index.lock cannot be created; .git is read-only in sandbox) · verification: `scripts/validate.sh` => PASS node --check public/app.js; PASS python3 -m py_compile pipeline/*.py; PASS JSON.parse public/data/*.json; PASS GE15 coalition tally · next: restore git write access and commit the validation-script task changes.
2026-06-27 16:04 UTC · Codex · files: NIGHT_QUEUE.md, AGENT_LOG.md · commit: BLOCKED (.git/index.lock cannot be created; .git is read-only in sandbox) · verification: no Claude commit present after base snapshot; reviewed `public/app.js`/`public/index.html` loadOptional path; `node --check public/app.js` PASS; `scripts/validate.sh` PASS all checks · next: loadOptional failure path confirmed intact: boundary `render()` completes before optional fetches, results/scores fetch failures are caught independently, and disabled mode buttons prevent unavailable hash modes from activating.

2026-06-27 16:20 UTC · Claude · files: public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 1a93061 · verification: CSS-only change (no JS/markup); `node --check public/app.js` PASS; styles.css braces balanced 94/94; new @media (max-width:480px) and (max-width:390px) blocks: search becomes a full-width row, tier/mode segs stretch to fill, results dropdown anchored left:0/right:0 (no horizontal overflow), brand tag hidden + tighter padding ≤390px. NOTE: scripts/validate.sh FAILs are environmental only (can't write logs to read-only /tmp in sandbox), not real failures. · next: i18n completeness pass (next unchecked (claude) task).

2026-06-27 16:40 UTC · Claude · files: public/index.html, public/app.js, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 0f7e90d · verification: `node --check public/app.js` PASS; EN/MS I18N key sets at parity and every data-i18n* key in index.html resolves (script check => [] missing both ways). Wired 5 previously-hardcoded user-facing strings through I18N: meta description (meta_desc), SVG aria-label (map_aria — was hardcoded BM "Peta kerusi Malaysia" even at EN default, now EN/BM), and the three control-group aria-labels Seat layer/Colour mode/Language (aria_tier/aria_mode/aria_lang). Added two new applyStatic handlers: data-i18n-aria→aria-label, data-i18n-content→content. · next: Accessibility pass (next unchecked (claude) task) — can build on data-i18n-aria for new aria-labels.

2026-06-27 17:05 UTC · Claude · files: public/index.html, public/app.js, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 6ce659e · verification: `node --check public/app.js` PASS; CSS braces balanced 98/98; EN/MS I18N at parity (45 keys each), all data-i18n* keys in index.html resolve. Accessibility pass: added aria-label to search input (#q, i18n search_aria) + #results listbox (results_aria); aria-label override on #reset (reset_aria, so the ↺ glyph isn't read literally); explicit aria-label on EN/BM lang buttons (English/Bahasa Melayu). Made role="tab" buttons announce state — added aria-selected to tier/mode/lang markup and a setOn(el,on) helper that keeps the "on" class + aria-selected in sync at all 5 toggle sites (setLang/setTier/setMode + 2 init). CSS: new :focus-visible ring (2px accent outline; inset -2px on .seg buttons since the segment clips overflow; re-asserted on .search input which zeroed its outline). Legend not colour-only: every swatch row already pairs with a text label; added a 1px ring to .legend/.sharebar-key swatches so low-contrast colours stay distinguishable. · next: Loading + empty states (next unchecked (claude) task).

2026-06-27 17:30 UTC · Claude · files: public/app.js, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: ef83ab9 · verification: `node --check public/app.js` PASS; CSS braces balanced 105/105; EN/MS I18N at parity (46 keys each), all data-i18n* keys in index.html resolve. Loading + empty states: (1) #loading text now has a subtle pulse affordance while boundaries load (CSS @keyframes loading-pulse, gated by prefers-reduced-motion). (2) Boundary fetch failure previously left the spinner hanging forever — render() is now wrapped in try/catch at init AND setTier; on failure showLoadError() swaps the overlay to a friendly i18n'd message (new load_error key EN+BM). setTier failure restores the previous tier + tab state (state.tier/buttons) so the old map stays usable; renderSummary() guards against missing data. All state swaps happen inside the existing position:absolute;inset:0;pointer-events:none overlay → zero layout shift. setLang re-applies the error copy after applyStatic so a language toggle during the error state stays translated. · next: queue has only (codex) hygiene task left for (claude) lane — no doable (claude) task remaining.

2026-06-27 18:00 UTC · Claude · files: public/index.html, public/app.js, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 0d8c197 · verification: `node --check public/app.js` PASS; `grep -rni "petayb\|peta yb" public/` => no user-facing leftovers; MyPolitik now in `<title>`, brand mark, and both EN/MS I18N `title` keys. Phase B brand rename → MyPolitik. Changed 4 user-facing strings + 1 source comment: index.html `<title>` "Peta YB —"→"MyPolitik —"; `.brand` mark `◧ PETA<b>YB</b>`→`◧ My<b>Politik</b>` (two-tone bold on "Politik"); app.js EN+BM I18N `title` keys (mirror the <title>, applyStatic sets document.title); app.js line-1 header comment. KEPT infra untouched: `localStorage` key `peta-yb-lang` (×2, would break saved lang pref) and the `brand_tag` taglines ("Your representatives, on the map" / "Wakil rakyat anda, atas peta" — descriptive, no brand-name leftover). #panel-empty held no brand string. [HVR] — structural/grep-verified only; brand-mark visual rendering is Danial's morning call. · next: codex VERIFY brand task, then Phase 0 keystone lib.js (next (claude) task).

2026-06-27 18:30 UTC · Claude · files: public/lib.js (new), public/lib.test.mjs (new), public/app.js, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 2b925a4 · verification: `node --test public/lib.test.mjs` => 11 pass / 0 fail; `node --check public/app.js` PASS; `node --check public/lib.js` PASS. Phase 0 keystone: created DOM-free ES module public/lib.js exporting pure `encodeHash(state)` (#tier/mode[/code], percent-encoded, falsy `selected` omitted) and `decodeHash(hash)` (strips leading #, null on empty, splits on "/", per-segment decodeURIComponent). Faithful extraction of app.js's old writeHash/parseHash internals — NO behaviour change: writeHash() now `encodeHash(state)`, parseHash() now `decodeHash(location.hash)`. app.js already had type="module" so the import needed no markup change. lib.test.mjs has 11 zero-dep cases incl. round-trip + the faithful quirk that absent trailing segments decode to `undefined` (not "") — old parseHash's `.map` only ran over present elements, so tests assert that, not a "fixed" empty-string. [MV] — machine-verified. · next: codex VERIFY+STRESS Phase 0 (break encode/decode with junk/empty/extra-field hashes), then Phase 1 geolocation core (next (claude) task).

2026-06-27 19:00 UTC · Claude · files: public/lib.js, public/lib.test.mjs, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 44472b6 · verification: `node --test public/lib.test.mjs` => 27 pass / 0 fail (11 prior + 16 new); `node --check public/app.js` PASS; `node --check public/lib.js` PASS. Phase 1 geolocation core. Added to lib.js: faithful `project(lng,lat)` (frozen krackedmaps constants verbatim from pipeline/01_boundaries.py INCL. the lng>=107 Borneo shift -240.0879); `parsePathRings(d)` (M/L/Z → rings of [x,y], tolerant of missing trailing Z + junk/null); `pointInRings(x,y,rings)` (even-odd ray-cast across all rings → holes handled by parity, matches SVG even-odd fill); bbox-prefiltered `findSeatForLocation(lat,lng,seats)` (project once → bbox skip → PIP, returns seat or null, NaN/non-array guarded); `haversine` + `nearestSeat(lat,lng,seats)` fallback over seat.ll. Tests load the REAL public/data/seats-parlimen.json and assert a 6-row fixture table resolves to the geographically-correct seat: KLCC→P.119 Titiwangsa, Putrajaya→P.125, Penang/George Town→P.049 Tanjong, JB→P.160, Kuching→P.195 Bandar Kuching, KK→P.172 Kota Kinabalu — the last two (lng 110/116) exercise the shift branch end-to-end. Plus unit tests: project viewBox bounds + a direct 107-boundary shift assertion (delta≈240.0879), parsePathRings happy/garbage, pointInRings square + hole, NaN/Paris→null, haversine KL→SG≈300km, offshore nearestSeat fallback, bad-input→null. No app.js changes (wiring is a later Phase-2 task). [MV] — machine-verified. · next: codex VERIFY+STRESS geolocation, then `pickInitialLang` pure fn (next (claude) task).

2026-06-27 19:30 UTC · Claude · files: public/lib.js, public/lib.test.mjs, public/app.js, NIGHT_QUEUE.md, AGENT_LOG.md · commit: c8eed18 · verification: `node --test public/lib.test.mjs` => 31 pass / 0 fail (27 prior + 4 new); `node --check public/app.js` PASS; `node --check public/lib.js` PASS. Phase 1 lang picker. Added pure `pickInitialLang(saved, navLanguages)` to lib.js: (1) explicit saved pref "ms"/"en" always wins; (2) else first recognised browser tag decides, case-insensitive, matching "ms"/"ms-*"/"ms_*" and "en"/"en-*"/"en_*", skipping unknown/non-string entries; (3) else BM-first default "ms". Wired boot: app.js now `lang = pickInitialLang(saved, navigator.languages)` inside the existing try/catch (catch falls back to pickInitialLang(null,null) → "ms"). This FLIPS the previous hardcoded default from "en" to BM-first "ms" when no saved pref + no en/ms browser tag — intended per queue ("ship the MECHANISM; final taste is Danial's"). 4 new tests: saved-wins, browser-decides (incl. case-insensitive + first-tag-wins + skip-unknown), BM-default fallback (empty/null/undefined/invalid-saved), non-string nav entries ignored. [MV] — machine-verified; the default-language taste is Danial's morning call. · next: codex VERIFY+STRESS lang picker, then Phase 1 card-format helpers (next (claude) task).

2026-06-27 20:00 UTC · Claude · files: public/lib.js, public/lib.test.mjs, NIGHT_QUEUE.md, AGENT_LOG.md · commit: cafa597 · verification: `node --test public/lib.test.mjs` => 44 pass / 0 fail (31 prior + 13 new); `node --check public/lib.js` PASS; `node --check public/app.js` PASS. Phase 1 card-format helpers. Added 4 pure DOM-free shapers to lib.js (no HTML — callers render, helpers only shape): `formatParty(r)` → {abbr, full, label} surfacing party_full ("Perikatan Nasional (PN)" when both differ, single value when same/one-missing, null when neither); `candidateCount(r)` → int>=1 surfacing n_candidates (truncates, rejects 0/non-number); `formatRunnerUp(r)` → {name, party, votes} surfacing runner_up.votes with per-field guards (null when no runner-up = single-candidate seat); `formatResultCard(r)` composes all + passes scalar numerics (votes/vote_pct/majority/majority_pct/turnout) through a Number.isFinite guard so legit 0s and 100% turnout survive where a falsy check would drop them. Two `str`/`num` module-local helpers. Tests load the REAL public/data/results-ge15.json: exact-shape assertion on P.001 Perlis fixture + a whole-dataset smoke loop (all 222 seats shape with name+party label+finite votes, no throw), plus synthetic edge cases (missing/partial/same party, single-candidate=no runner-up, 0 votes, 0% majority, 100% turnout, garbage/null input). No app.js wiring — renderPanel enrichment is the later Phase-2 task; these helpers are its building blocks. [MV] — machine-verified. · next: codex VERIFY+STRESS formatters (missing runner-up / 0 votes / 100% turnout / single-candidate), then Phase 2 "Find your YB" front door (next (claude) task).

2026-06-27 20:30 UTC · Claude · files: public/index.html, public/app.js, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 514ea02 · verification: `node --check public/app.js` PASS; CSS braces balanced 119/119; EN/MS I18N at parity (51 keys each); all 29 data-i18n* markup keys resolve in I18N; old empty_h/empty_p confirmed removed from both langs. Phase 2 "Find your YB" front door. Rewrote #panel-empty into a citizen-first action card: `.find-kicker` "FIND YOUR YB", `<h2>` "Who represents you?", `.find-sub` one-liner, a prominent full-width `#find-location` "📍 Use my location" button (var(--accent) fill, min-height:44px tap target), a "or search by name above" prompt + plain seat examples ("Titiwangsa"/"Bangi"/"Kota Kinabalu"). Demoted legend+summary into a collapsible `<details id="empty-more">` ("Explore the whole map", custom ▸/▾ marker) — KEPT the `#legend`/`#summary` element IDs so renderSummary() needs NO change and still populates them. Added 7 EN+BM I18N keys (find_kicker/find_h/find_sub/find_loc/find_or/find_eg/empty_more); removed the now-unused empty_h/empty_p from both langs. CSS: new `.find*` hero block + `.empty-more` disclosure styles in the panel section. NOTE: `#find-location` is intentionally UNWIRED this round — the geolocation→findSeatForLocation→select() wiring is the explicit next (claude) Phase-2 task; the button is inert until then. [HVR] — structural/grep/parity-verified only; visual layout + copy taste is Danial's morning call. · next: codex VERIFY front door (node --check; every new I18N key in BOTH en+ms — done, confirm; no untranslated strings), then Phase 2 wire geolocation button (next (claude) task).

2026-06-27 21:00 UTC · Claude · files: public/app.js, public/index.html, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 812979d · verification: `node --check public/app.js` PASS; `node --check public/lib.js` PASS; `node --test public/lib.test.mjs` => 44 pass / 0 fail (unchanged — no new pure logic, wiring only); CSS braces balanced 121/121; EN/MS I18N at parity (in-en-not-ms [] and in-ms-not-en [] both empty), all 5 new loc_ keys present in BOTH langs; all data-i18n* markup keys resolve. Phase 2 geolocation wiring. Imported findSeatForLocation + nearestSeat from lib.js. New `locate()` handler on #find-location: guards `navigator.geolocation` (→loc_unsupported), disables the button + shows loc_locating while pending, getCurrentPosition(highAccuracy, 10s timeout, 5min maxAge) → findSeatForLocation on the CURRENT tier's seats, PIP-miss falls back to nearestSeat, then existing select(seat.code) (reuses zoom + panel + hash). Error cb: code===1 → loc_denied, else loc_error. No-match → loc_notfound. Status surfaced in new `<p id="find-status" role="status" aria-live="polite">` inside .find — setFindStatus(key) stores key in dataset.i18n so setLang→applyStatic re-translates it; cleared on deselect() to drop stale messages. The search box + examples above remain the always-present manual fallback; every failure copy points back to it ("…search by name above instead"). CSS: .find-loc:disabled (cursor:progress;opacity:.7), .find-status pill. [HVR] — structural/grep/parity-verified only; the geolocation permission flow + visual states are Danial's morning call (no browser in sandbox). · next: codex VERIFY+STRESS geo wiring (denied/unsupported/timeout paths; fallback always reachable; node --check), then Phase 2 enrich renderPanel + Share/Copy-link button (next (claude) task).

2026-06-27 21:30 UTC · Claude · files: public/app.js, public/index.html, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: d6c1aff · verification: `node --check public/app.js` PASS; `node --check public/lib.js` PASS; `node --test public/lib.test.mjs` => 44 pass / 0 fail (no new pure logic — renderPanel wiring reuses the Phase-1 helpers); CSS braces balanced 129/129; EN/MS I18N at parity (61 keys each, in-en-not-ms [] / in-ms-not-en []); all 5 new keys (candidates/share_btn/share_ok/share_fail/src_ge15) present in BOTH langs; all data-i18n* markup keys resolve. Phase 2 renderPanel enrichment + Share/Copy-link. Imported formatParty/candidateCount/formatRunnerUp from lib.js and wired them into renderPanel: (1) party row now shows party_full label ("Perikatan Nasional (PN)" via formatParty, only when it adds info beyond the bloc pill — skips redundant "PN · PN"); (2) new "Candidates"/"Calon" row from candidateCount; (3) runner-up row now appends runner_up.votes (formatRunnerUp), with the pill guarded when party is null. Share: full-width `#share-link` button in a new `.seat-actions` block; delegated click on PANEL_SEAT (innerHTML rebuilds each render) → shareLink() builds `location.origin+pathname+encodeHash(state)` deep-link → guarded `navigator.clipboard.writeText` → transient `#toast` (share_ok / share_fail copy points to URL bar on failure). Transparency: per-card `.src-line` "Results: official GE15 (SPR · DOSM/Thevesh)" shown whenever results are present. CSS: .src-line, .seat-actions, .share-btn (44px tap target, accent hover), .toast (fixed bottom-center pill, .show transition, reduced-motion guarded). Share button label re-translates on setLang via the existing renderPanel re-render. [HVR] — structural/grep/parity/test-verified only; clipboard permission flow + visual layout/toast are Danial's morning call (no browser in sandbox). · next: codex VERIFY share/deep-link (hash round-trip via lib.test; open-on-load selects the seat; clipboard guarded), then Phase 2 onboarding clarity (next (claude) task).

2026-06-27 22:00 UTC · Claude · files: public/index.html, public/app.js, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 75f747b · verification: `node --check public/app.js` PASS; `node --check public/lib.js` PASS; `node --test public/lib.test.mjs` => 44 pass / 0 fail (no new pure logic — UI/wiring only); CSS braces balanced 145/145; EN/MS I18N parity (in-en-not-ms [] / in-ms-not-en []); 3 new keys (mode_soon, tap_hint, tap_hint_dismiss) each present in BOTH langs; all data-i18n* markup keys resolve. Phase 2 onboarding clarity. (1) Skor "Soon/Segera" pill: the gated Score tab no longer reads as a dead button — a small ::after badge (CSS `.seg button[data-after]::after`, accent-2 fill, guarded on [data-after] so an unset attr never paints an empty dot) shows "Soon"/"Segera". Driven by new `data-i18n-after="mode_soon"` on the button + a new applyStatic handler that sets `data-after` to the translated text (pseudo-element, so the button's textContent i18n can't clobber it). Tab STAYS disabled — gating unchanged. (2) First-load tap hint: new `#tap-hint` pill over the map (i18n tap_hint) nudges that seats are tappable; dismissed by its ✕, by select() (any seat), or remembered in localStorage key `peta-yb-hint-seen` (guarded try/catch; hintSeen/dismissHint/maybeShowHint). maybeShowHint() runs at end of init only when not seen AND nothing selected — a deep-linked seat skips the hint. (3) Reset discoverability: `#reset:not([hidden])` now slides+fades in (@keyframes reset-in) each time "Show all" appears so the way back catches the eye. All three animations gated by prefers-reduced-motion (tap-hint keeps a static translateX(-50%) centering fallback). [HVR] — structural/grep/parity/test-verified only; visual placement, copy taste, and the geolocation/localStorage flows are Danial's morning call (no browser in sandbox). · next: codex VERIFY onboarding (Skor stays gated even if scores.json present; localStorage guarded), then Phase 2 mobile-first sheet (next (claude) task).

2026-06-27 22:30 UTC · Claude · files: public/index.html, public/app.js, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 7773916 · verification: `node --check public/app.js` PASS; `node --check public/lib.js` PASS; `node --test public/lib.test.mjs` => 44 pass / 0 fail (no new pure logic — UI/wiring only); CSS braces balanced 154/154; EN/MS I18N parity (65 keys each, in-en-not-ms [] / in-ms-not-en []); 1 new key (sheet_handle) present in BOTH langs; all 33 data-i18n* markup keys resolve. Phase 2 mobile-first sheet. On screens ≤820px the panel is now a draggable bottom sheet OVERLAYING the map (the map is the reward): grid drops the "panel" row so #stage takes the full height; #panel goes `position:fixed; bottom:0; height:85dvh` (85vh fallback) with a `transform:translateY(calc(100% - var(--sheet-peek)))` peek (--sheet-peek:172px, --sheet-pad:16px on #app so #reset can inherit the peek too). `.sheet-open` slides it to translateY(0); `.dragging` drops the transition for finger-follow. New full-bleed sticky `#sheet-handle` button (min-height:44px tap target, touch-action:none, 40×4 grip pill) toggles via tap/keyboard (click→setSheet, suppressed right after a drag) AND pointer-drag (pointerdown/move/up/cancel: live inline translateY clamped [0, height−peek], snaps open/closed at the halfway mark on release). select() calls setSheet(true) so a tapped/deep-linked/located seat opens the card; deselect() setSheet(false) collapses to peek. #reset floats above the peek (`bottom:calc(var(--sheet-peek)+12px)`). Removed the old `max-height:42vh/46vh` panel rules; ≤480 now just overrides `--sheet-pad:14px` + `#reset left:12px`. Desktop (>820px) is fully unaffected: `.sheet-handle{display:none}` base rule and no desktop `.sheet-open` rule, so setSheet calls are inert there. Safe-area-inset-bottom padding for notched phones; prefers-reduced-motion drops the slide transition. [HVR] — structural/grep/parity/test-verified only; the peek height, default-collapsed-on-load choice, drag feel, and visual layout are Danial's morning call (no browser in sandbox). · next: codex VERIFY mobile (42vh truncation fixed, tap-target sizes, no horizontal overflow at ≤390px; review pointer-drag + reduced-motion paths), then Phase 3 share IMAGE drawSeatCard (next (claude) task).

2026-06-27 23:00 UTC · Claude · files: public/lib.js, public/lib.test.mjs, public/app.js, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: e20e75f · verification: `node --test public/lib.test.mjs` => 48 pass / 0 fail (44 prior + 4 new fitBox); `node --check public/app.js` PASS; `node --check public/lib.js` PASS; CSS braces balanced 154/154; EN/MS I18N parity (68 keys each, both diffs []); all data-i18n* markup keys resolve. Phase 3 share IMAGE drawSeatCard. (1) Added pure `fitBox(bbox, targetW, targetH, pad)` to lib.js — the share-card thumbnail transform: returns {scale, dx, dy} to fit a seat bbox into a target rect (aspect-preserving + centred), null on degenerate bbox / non-positive target / pad-eats-box / NaN. 4 new zero-dep tests: square-in-wide, padded non-zero-origin, a REAL P.001 bbox staying inside a 1080×760 padded region, and the degenerate/garbage→null table. (2) app.js: imported fitBox; new `drawSeatCard(seat)` paints an off-screen 1080×1350 canvas — accent header band (partyColor of the bloc, neutral when no result), `◧ MyPolitik` brand, the seat silhouette via `new Path2D(seat.d)` translated/scaled by fitBox + filled evenodd (try/caught so a Path2D-less engine still yields a text card), kicker·code, width-clamped seat name, state, YB name + rounded bloc pill + majority (or rep_ph when no GE15 row), and a `card_find <host>` CTA + src_ge15 footer. `shareCard()` awaits `document.fonts.ready` (guarded), `toBlob`→PNG `File`, prefers `navigator.canShare({files})`+`navigator.share` (AbortError = user-dismissed, silent), else an object-URL `<a download>` fallback; any throw → `card_fail` toast pointing to the 🔗 link. Re-entrancy-guarded (sharingCard). New `#share-card` button added to the existing `.seat-actions` (now flex row, .share-btn flex:1 1 140px so the two buttons share the row). 3 new I18N keys EN+BM (card_btn, card_fail, card_find). (3) styles.css: .seat-actions → flex/gap/wrap. [HVR] — fitBox is machine-verified; the Canvas rendering, share-sheet flow, fonts.ready timing and visual layout need a real browser → Danial's morning call (no browser/canvas in sandbox). · next: codex VERIFY card image (bbox→thumbnail transform unit-tested — done via fitBox, confirm; graceful Copy-link fallback when share unsupported), then Phase 4 regression / ♻️ critique-and-replenish if lists drain.

2026-06-27 23:30 UTC · Claude · files: NIGHT_QUEUE.md, AGENT_LOG.md · commit: 0459fde · verification: ♻️ CRITIQUE & REPLENISH round. FULL REGRESSION GREEN: `node --test public/lib.test.mjs` => 48 pass / 0 fail; `node --check public/app.js` PASS; `node --check public/lib.js` PASS; `python3 -m py_compile pipeline/*.py` PASS. Audit found NO bugs: I18N EN/MS parity 69/69 (diffs empty modulo a value-string false-positive); all 33 data-i18n* markup keys resolve in en; seat data clean (222 parlimen + 600 DUN, every seat has d/bbox/ll/code — 0 missing); GE15 results have 0 partial rows (no vote_pct-without-votes / votes-without-pct / majority-without-pct); share buttons delegated correctly on PANEL_SEAT; geolocation locate() guards unsupported/denied/error/notfound + always points back to search; setTier restores prev tier+tabs on boundary-load failure; deep-link to skor stays gated (setMode honours b.disabled). Per the perpetual-tail mandate (no bugs → improvements ordered) appended a new "Phase 5 — Critique round 1" section ABOVE the ♻️ line: 3 (claude) build tasks + 3 paired (codex) verify tasks — (1) [MV] port partyColor/COALITION_COLORS to lib.js + tests (testable-foundation regime), (2) [MV] harden renderPanel numeric rows via formatResultCard's finite guards so partial/future data can't render "NaN" + test, (3) [HVR] search-results keyboard nav (↑/↓ highlight + Enter-selects-highlighted + aria-activedescendant) to finish the a11y pass. Did NOT implement them (one task per round; ♻️ replenish IS this round's task). Left the ♻️ tail line in place per rules. · next: top (claude) task is now Phase 5 task 1 (port partyColor to lib.js + tests).

2026-06-27 23:50 UTC · Claude · files: public/lib.js, public/lib.test.mjs, public/app.js, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 37003cf · verification: `node --test public/lib.test.mjs` => 52 pass / 0 fail (48 prior + 4 new); `node --check public/app.js` PASS; `node --check public/lib.js` PASS. Phase 5 task 1 — port colour mapping to lib.js. Moved `COALITION_COLORS` (9-entry GE15 swatch table, byte-identical to the old inline) + `partyColor(p)` from app.js (~40-46) into public/lib.js as exported pure members; app.js now imports `partyColor` (added to the existing lib.js import list) and keeps `COALITION_ORDER` (still used at app.js:632 renderSummary). One tested source of truth now feeds legend bars / bloc pills / seat fill / share-card. Behaviour for valid strings UNCHANGED (same uppercased lookup, same #5d6b7d fallback) — the lib version additionally hardens against non-string input via a `typeof p === "string"` guard (old `(p||"").toUpperCase()` would throw on a number/object; new returns the fallback), matching the task's junk/mixed-case/non-string requirement. 4 new zero-dep tests: all 6 GE15 coalitions (PH/PN/BN/GPS/GRS/WARISAN) → exact colour; case-insensitive (ph/Warisan/Bn); unknown/empty/null/undefined/no-arg/number/object/array → #5d6b7d; deepEqual lock on the full COALITION_COLORS table (no swatch drift). [MV] — machine-verified. · next: codex VERIFY+STRESS partyColor port (byte-identical colours, no legend/pill/share-card drift; junk/mixed-case/non-string → fallback), then Phase 5 task 2 (harden renderPanel numeric rows via formatResultCard finite guards + test) is the next (claude) task.

2026-06-28 00:10 UTC · Claude · files: public/app.js, public/lib.test.mjs, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 5dc216f · verification: `node --test public/lib.test.mjs` => 53 pass / 0 fail (52 prior + 1 new); `node --check public/app.js` PASS; `node --check public/lib.js` PASS. Phase 5 task 2 — harden renderPanel numeric rows. renderPanel now drives its numeric rows off `formatResultCard(r)` (imported in place of formatParty/candidateCount/formatRunnerUp, which it composes) instead of reading raw `r.majority`/`r.votes`/`r.vote_pct`/`r.turnout` + bare `Number(...)`. Every numeric passes lib.js's `Number.isFinite` guard (non-finite → null), so each row is gated on `card.<field> != null`: a partial row (vote_pct present but votes missing, NaN majority, string turnout) now OMITS the row rather than rendering "NaN"/"undefined". Behaviour on real (complete) GE15 data is unchanged — all 222 rows have finite numerics. Side note: the win_votes row now gates on `votes` (was `vote_pct`) with the pct shown conditionally, and majority-pct/runner-up-votes use the guarded card fields. New lib.test case "formatResultCard: non-finite numerics drop to null" asserts vote_pct-without-votes, NaN, string and Infinity numerics all become null (the panel's omit signal), while the lone finite vote_pct (53.2) survives. [MV] — machine-verified; real GE15 data is complete today so this is defensive for future/DUN result data (visual unchanged → no browser needed). · next: codex VERIFY+STRESS panel hardening (synthetic pct-without-votes / majority-without-pct / NaN/string rows render clean or omit, never NaN/undefined; node --check), then Phase 5 task 3 (search-results keyboard nav) is the next (claude) task.

2026-06-28 00:30 UTC · Claude · files: public/index.html, public/app.js, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: c59c3e0 · verification: `node --check public/app.js` PASS; `node --check public/lib.js` PASS; `node --test public/lib.test.mjs` => 53 pass / 0 fail (unchanged — UI/wiring only, no new pure logic); CSS braces balanced 155/155. Phase 5 task 3 — search-results keyboard nav (a11y pass completion). #results listbox is now keyboard-navigable: new activeResult index + setActiveResult(idx) toggles a `.active` highlight + aria-selected on each option and mirrors the focused option's id to aria-activedescendant on #q (cleared when idx<0); moveActiveResult(±1) wraps at both ends. Q keydown: ArrowDown/ArrowUp preventDefault + move (no-op when list hidden), Enter selects the HIGHLIGHTED option (opts[activeResult]) falling back to opts[0] when none highlighted (was: always first), Escape unchanged (clear). New hideResults() centralises RESULTS.hidden=true + setActiveResult(-1) + aria-expanded=false and replaces the 4 raw `RESULTS.hidden=true` sites (input render-reset, result click, Escape, outside-click) so activedescendant/expanded never go stale. Markup: #q gains role=combobox/aria-autocomplete=list/aria-controls=results/aria-expanded; each rendered option gets id=result-opt-N + role=option + aria-selected=false; input handler sets aria-expanded=true on show. CSS: `.results button.active` mirrors :hover bg + an inset 2px accent bar. No I18N changes (no new strings). [HVR] — structural/syntax/test-verified only; the focus-ring visuals, scrollIntoView feel and SR announcement are Danial's morning call (no browser in sandbox). · next: codex VERIFY search keyboard nav (↑/↓ wrap, Enter picks highlighted, Escape clears, no JS error on empty results, aria-activedescendant clears when dropdown hides), then ♻️ critique-and-replenish (Phase 5 (claude) build tasks now drained).

2026-06-27 (night) · Claude · files: NIGHT_QUEUE.md, AGENT_LOG.md · commit: (this commit) · task: ♻️ PERPETUAL TAIL — critique & replenish (Phase 5 (claude) tasks all done; only (codex) verify + the tail remained). · verification: FULL REGRESSION GREEN → `node --test public/lib.test.mjs` = 53 pass / 0 fail; `node --check public/app.js` OK; `python3 -m py_compile pipeline/*.py` OK. Audited lib.js + app.js against the AGENTS.md vision. · FOUND 1 BUG (recorded as top fix in new Phase 6, NOT fixed this round per protocol): stale-result Enter — `hideResults()` never clears `RESULTS.innerHTML` and the `#q` Enter handler lacks a `RESULTS.hidden` guard, so Enter after Escape / after a selection re-clicks `opts[0]` and reselects a stale seat. · Replenished Phase 6 with 3 (claude) build tasks + 3 (codex) verifies: (1) fix stale-Enter, (2) extract pure `scoreColor()` to lib.js + tests, (3) confirm `#q` combobox ARIA attributes. · next: top doable (claude) task = FIX stale-result Enter (Phase 6).

2026-06-27 17:00 UTC · Claude · files: public/app.js, NIGHT_QUEUE.md, AGENT_LOG.md · commit: d2defff · verification: `node --check public/app.js` PASS; `node --test public/lib.test.mjs` 53/53 pass. Fixed Phase 6 stale-result Enter bug: (1) added `if (RESULTS.hidden) return;` guard to the #q Enter keydown branch (app.js ~755) so it mirrors ArrowUp/Down — Enter after Escape or after a selection no longer re-clicks opts[0]; (2) hideResults() now sets `RESULTS.innerHTML = ""` so stale option buttons can't be announced by SR or clicked. [HVR] note: logic is structurally sound + machine-checked, but the keyboard/SR behaviour itself is Danial's visual/AT call. · next: Phase 6 next (claude) task — extract scoreColor() ramp from seatValueColor into lib.js with tests.

2026-06-27 · Claude · files: public/lib.js, public/app.js, public/lib.test.mjs, NIGHT_QUEUE.md · commit: c292d22 · verification: `node --check public/app.js` PASS; `node --test public/lib.test.mjs` 56 pass / 0 fail (3 new scoreColor cases). Ported the inline 0..100 red→green hsl ramp from seatValueColor (app.js ~273-276) into pure `scoreColor(score)` in lib.js; app.js now imports it — no behaviour change for valid scores (byte-identical hsl at 0/25/50/75/100). Junk/NaN/Infinity/string/null/out-of-range now clamp or fall back to #5d6b7d instead of emitting `hsl(NaN …)`. · next: (codex) [MV] VERIFY+STRESS scoreColor port — confirm byte-identical ramp + fallbacks; then (claude) [HVR] combobox a11y attrs on #q.

2026-06-27 16:40 UTC · Claude · files: public/index.html, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 9430450 · verification: `node --check public/app.js` PASS. Confirmed `#q` already carried role="combobox", aria-autocomplete="list", aria-controls="results" (matches `#results` id), and initial aria-expanded="false"; options carry role="option"+id (app.js:731); JS toggles aria-expanded (app.js:712/735) and aria-activedescendant (app.js:696/698). Added the one missing standards-recommended attribute aria-haspopup="listbox" so screen readers explicitly announce the listbox popup type — completes the keyboard-nav a11y pass. HVR: markup/ARIA only, no behaviour change; final screen-reader check is Danial.s morning call. · next: top unchecked (claude) task is the ♻️ perpetual tail (Phase 6 claude tasks now drained) — critique & replenish.

2026-06-27 20:06 UTC · Claude · files: NIGHT_QUEUE.md, AGENT_LOG.md · commit: a2422c9 · task: ♻️ PERPETUAL TAIL (regression + audit + replenish — NOT ticked, by design) · verification: FULL REGRESSION GREEN — `node --test public/lib.test.mjs` 56/56 pass; `node --check public/app.js` OK; `node --check public/lib.js` OK; `python3 -m py_compile pipeline/*.py` OK. Audit: I18N en/ms parity verified (68 keys each, no orphans); toast/find-status already have role=status+aria-live; search listbox a11y (aria-selected/aria-activedescendant) correct; share buttons use PANEL_SEAT delegation (no listener leak); boot ignores bad seat codes (byCode.has guard); setMode guards disabled/gated modes; runner-up pill colour confirmed correct (GE15 party normalised to coalition, so partyColor(ru.party) resolves). No bugs found. · Replenished Phase 7 (3 claude build + 3 codex verify): (1) [MV] nearestSeat maxKm guard so out-of-country geolocation shows honest loc_notfound instead of snapping to a random border seat; (2) [MV] extract I18N → public/i18n.js + parity test (guard untranslated-key regressions); (3) [HVR] writeHash() after boot so a gated deep-linked mode (#parlimen/skor) normalises the shared URL. · next: top (claude) task is now Phase 7's nearestSeat distance guard [MV].

2026-06-27 · Claude · files: public/lib.js, public/app.js, public/lib.test.mjs, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 559d5c1 · verification: `node --test public/lib.test.mjs` => 58 pass / 0 fail (+2 new: maxKm offshore-matches / London→null / default-unchanged); `node --check public/app.js` PASS; `node --check public/lib.js` PASS. Phase 7 task 1 (geo distance guard): added optional `maxKm=Infinity` param to `nearestSeat(lat,lng,seats,maxKm)` returning null when closest rep-point > maxKm (no behaviour change for existing callers/tests); `locate()` now passes 150 km so out-of-country users (Singapore/abroad) get `loc_notfound` + reachable search fallback instead of snapping to a random border seat. · next: next unchecked (claude) Phase 7 task — I18N parity foundation (extract I18N into public/i18n.js + identical-key-set + non-empty-string tests).

2026-06-27 (night) · Claude · files: public/i18n.js (new), public/app.js, public/lib.test.mjs, NIGHT_QUEUE.md, AGENT_LOG.md · commit: fe9c460 · verification: `node --test public/lib.test.mjs` => 60 pass / 0 fail (was 56; +4: 2 new i18n parity tests + existing); `node --check public/app.js` PASS; `node --check public/i18n.js` PASS; `node --check public/lib.js` PASS. Strings verified BYTE-IDENTICAL via `git show HEAD:public/app.js` diff (STRINGS BYTE-IDENTICAL). Phase 7 [MV] I18N parity foundation: extracted the `I18N` string table out of app.js into a new DOM-free static ES module `public/i18n.js` exporting `I18N` (plain module like lib.js — zero new deps, no build step); app.js now `import { I18N } from "./i18n.js"` (added below the lib.js import) and the ~92-line inline table was replaced with a 3-line pointer comment. No behaviour change — same keys, same strings. Added two lib.test.mjs cases: (1) `I18N.en` and `I18N.ms` have IDENTICAL sorted key sets (catches "added an EN key, forgot the BM translation"), (2) every value in both locales is a non-empty / non-whitespace-only string. Guards the untranslated-key regression class on the tested foundation. · next: codex VERIFY i18n parity foundation (confirm node --check, byte-identical strings, that the parity test fails if a BM key is deleted); then next (claude) Phase 7 task is the [HVR] honest deep-link hash (writeHash() after boot mode restore).

2026-06-27 · Claude · files: public/app.js, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 2a7d9a1 · verification: `node --check public/app.js` PASS; `node --test public/lib.test.mjs` 56 tests, fail 0. Phase 7 "Honest deep-link hash" — added a single `writeHash()` after the boot mode/code restore (app.js init, ~861) so the URL normalises to the actually-active state: a deep link to a gated mode (#parlimen/skor) or a bad seat code now drops the stale hash instead of misrepresenting shared state (setMode()/select() short-circuit on disabled/missing and skip their internal writeHash). Uses replaceState → no extra history entry. [HVR] structural sanity only (headless box, no browser): logic verified by reading the short-circuit paths; URL-normalisation behaviour is Danial's morning visual call. · next: (codex) VERIFY honest deep-link hash — load #parlimen/skor → URL normalises, valid #dun/parti/<code> still selects+keeps hash, no duplicate history entry.

2026-06-27 20:35 UTC · Claude · files: NIGHT_QUEUE.md, AGENT_LOG.md · commit: e4d1971 · task: ♻️ CRITIQUE & REPLENISH (perpetual tail — NOT ticked) · verification: FULL REGRESSION GREEN — `node --test public/lib.test.mjs` => 60 pass / 0 fail; `node --check` clean on public/app.js + public/lib.js + public/i18n.js; `python3 -m py_compile pipeline/*.py` OK. AUDIT found NO bugs. Investigated two suspected issues and confirmed both are NON-bugs against real data: (1) renderPanel runner-up pill `partyColor(ru.party ...)` — GE15 `runner_up.party` is a COALITION code ("BN" in P.001), so it colours correctly; (2) DUN search-by-code — DUN `code` ("10_N.01") contains the visible `dun_code` ("N.01") as a substring, so the existing `.includes()` filter already matches. REAL GAPS found & queued (no regressions, hardening/craft): the core search filter (app.js ~631) has NO unit tests, and the `<head>` lacks Open Graph / Twitter Card tags for shareable-link previews (the growth engine). · Appended Phase 8 (2 claude build + 2 codex verify pairs) above the perpetual tail. · next: top of Phase 8 — extract pure `searchSeats()` into lib.js with tests (claude [MV]).

2026-06-27 17:?? UTC · Claude · files: public/lib.js, public/app.js, public/lib.test.mjs, NIGHT_QUEUE.md, AGENT_LOG.md · commit: e8167ed · verification: `node --test public/lib.test.mjs` => 69 tests pass (9 new searchSeats cases); `node --check public/app.js` + `node --check public/lib.js` PASS; parity check vs old inline filter on real seats-parlimen.json => IDENTICAL code lists for sungai/p.1/selangor/bangi/johor/xyz. Extracted the inline #q search filter into pure `searchSeats(seats,query,tier)` in lib.js (case-insensitive name/code/state substring; DUN tier ALSO matches dun_code + parent parlimen code; non-array/null/blank/non-string query → [] no throw); app.js imports + calls it (behaviour unchanged on parlimen, additive on DUN). · next: Phase 8 (claude) [HVR] link-preview growth — add OG/Twitter Card + theme-color meta to <head>, wire og:title/description to I18N via data-i18n-content.

2026-06-27 20:51 UTC · Claude · files: public/index.html, public/assets/og-card.svg (new), public/assets/icon.svg (new), NIGHT_QUEUE.md · commit: 2210499 · verification: node --check public/app.js PASS; node --test public/lib.test.mjs 69/69 PASS; both new SVGs well-formed (start <svg>/end </svg>); og:image path resolves to a real file (public/assets/og-card.svg, 1076B); title+meta_desc present in BOTH en+ms (i18n.js); exactly one <title> + one <meta name="description"> (no dupes). Tags added to <head>: theme-color (#0b0e13), <link rel=icon> (assets/icon.svg), og:type(website), og:site_name(MyPolitik), og:title (data-i18n-content=title), og:description (data-i18n-content=meta_desc), og:image (assets/og-card.svg), twitter:card(summary_large_image), twitter:title (data-i18n-content=title), twitter:description (data-i18n-content=meta_desc), twitter:image. NOTE [HVR]: og:image is a placeholder branded SVG (1200×630) — Danial should swap a proper 1200×630 PNG social card (SVG og:images render inconsistently in some scrapers); the rendered link preview is his eye to confirm. · next: Phase 8 codex VERIFY link-preview meta (og/twitter well-formed, og:image resolves, data-i18n-content keys in both langs, theme-color present, no dup title/desc, node --check).

2026-06-27 21:?? UTC · Claude · files: NIGHT_QUEUE.md, AGENT_LOG.md · commit: 01a3136 · task: ♻️ CRITIQUE & REPLENISH (perpetual tail — NOT ticked, left in place per rules) · verification: FULL REGRESSION GREEN — `node --test public/lib.test.mjs` => 69 pass / 0 fail; `node --check` clean on public/app.js + public/lib.js + public/i18n.js; `python3 -m py_compile pipeline/*.py` OK; `JSON.parse` OK on all 3 public/data/*.json; GE15 coalition tally verified PH82·PN74·BN30·GPS23·GRS6·WARISAN3 (+BEBAS2·KDM1·PBM1) = 222. NOTE: scripts/validate.sh reports FAIL on every check but ONLY because it cannot write its `/tmp/peta-yb-*.log` files in this sandbox (Permission denied — pre-owned by another user); each underlying check passes when run directly (confirmed above). AUDIT found ONE defensive bug + tested-foundation/DRY gaps. BUG (queued at TOP): the `#q` search-input handler (app.js ~630) reads `state.data[state.tier].seats`; on a boundary-load failure boot shows the error overlay and returns early leaving `state.data[state.tier]` undefined while `#q` stays interactive → first keystroke throws an uncaught `TypeError: Cannot read properties of undefined (reading 'seats')`. The tooltip mousemove handler shares the shape but is safe (no `.seat` paths exist when render failed). GAPS (Phase 9): the `tier==="parlimen"?seat.code:seat.parlimen` RESULT join-key is duplicated 5× + the `seat.dun_code` DISPLAY code 2× (untested pure logic → extract `resultKey`/`displayCode` to lib.js + tests); the legend bloc counter (app.js ~542) is a pure aggregation that should pin the AGENTS.md UNTOUCHABLE 222-seat tally in a node --test-native unit test (validate.sh's tally check can't write its log here). Appended a top "🔝 Bug fixes" section (1 claude fix + codex verify) and "Phase 9 — Critique round 5" (2 claude build + 2 codex verify pairs) ABOVE the ♻️ line. Did NOT implement them (one task per round; ♻️ replenish IS this round's task). · next: top (claude) task is now the search-throw FIX (app.js ~630 `if (!data) return;`).

2026-06-27 (night) · Claude · files: public/app.js, NIGHT_QUEUE.md, AGENT_LOG.md · commit: f3ef184 · verification: `node --check public/app.js` PASS; `node --test public/lib.test.mjs` PASS (fail 0). FIXED search-throw on boundary-load failure (critique round 5 top bug): added `if (!data) return;` to the `#q` input handler (~631) immediately after `const data = state.data[state.tier];`, mirroring renderSummary's existing guard — so when init() bails after a failed render(tier) (boundaries file missing/corrupt) the still-interactive search box degrades to inert instead of throwing `TypeError: Cannot read properties of undefined (reading 'seats')` on first keystroke. ALSO guarded the tooltip `mousemove` handler (~562) with the same `if (!data) return;` for symmetry (it was already safe — no `.seat` paths exist when render failed — but the guard is cheap and consistent). · next: (codex) VERIFY+STRESS search-throw fix, then top (claude) task is Phase 9 resultKey/displayCode extraction into lib.js.

2026-06-27 (night) · Claude · files: public/lib.js, public/app.js, public/lib.test.mjs, NIGHT_QUEUE.md · commit: 3fcc5c7 · task: Phase 9 — extract resultKey/displayCode seat-key helpers (DRY + tested foundation). Added pure `resultKey(seat, tier)` (parlimen→`seat.code`, dun→`seat.parlimen`) and `displayCode(seat, tier)` (parlimen→`seat.code`, dun→`seat.dun_code`) to public/lib.js — both treat any non-"parlimen" tier as dun (matches the existing `=== "parlimen"` checks) and return `undefined` (no throw) for missing/non-object seat or missing field. Imported into app.js and replaced ALL clean inline ternaries: resultKey at 5 sites (seatValueColor parti+skor ~176/181, renderPanel ~276, drawSeatCard ~381, tooltip mousemove ~567 — the two function-local `const resultKey` were renamed to `rk` to avoid shadowing the import) and displayCode at 2 sites (tooltip ~566, search dropdown ~641). NO behaviour change — same value at every call site. The kicker label ternaries (~275/422) were intentionally LEFT: they embed the code inside a longer template string ("Parlimen · P.xxx" / "DUN · N.xx"), not a clean assignable code, so they're out of scope for these helpers. verification: `node --check public/app.js` PASS; `node --check public/lib.js` PASS; `node --test public/lib.test.mjs` => 76 pass / 0 fail (was 69; +7 new resultKey/displayCode cases covering parlimen/dun tiers, non-parlimen-string-as-dun, missing-field→undefined, non-object seat→undefined). grep confirms no `=== "parlimen" ? seat`/`? s.` clean ternaries remain. · next: (codex) VERIFY+STRESS resultKey/displayCode port (byte-identical keys on real parlimen+DUN data, missing fields no throw); then top (claude) task is Phase 9 tallyCoalitions extraction + GE15 222-tally unit test.

2026-06-27 18:05 UTC · Claude · files: public/lib.js, public/app.js, public/lib.test.mjs, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 312227c · verification: `node --test public/lib.test.mjs` 79 pass / 0 fail; `node --check public/app.js` + `node --check public/lib.js` OK. Extracted renderSummary's inline legend bloc counter (app.js ~542 `for…counts[v.coalition]=…`) into pure `tallyCoalitions(results)` in lib.js — returns `{coalition:count}`; null/non-object → `{}`; skips rows with missing/blank/non-string coalition (defensive — real GE15 data is complete so legend output is byte-identical). app.js now imports + calls it (one-line replacement, same `total`/`ordered`/bar/key rendering). Added 3 lib.test cases: (1) the AGENTS.md UNTOUCHABLE GE15 invariant — loads results-ge15.json and asserts PH82·PN74·BN30·GPS23·GRS6·WARISAN3 = 222 grand total (node-native guard since validate.sh's tally check can't write its /tmp log in this sandbox); (2) null/undefined/42/"x"/[]/{} → {} no throw; (3) synthetic rows with blank/whitespace/missing/null/non-string coalition skipped → only valid blocs counted. · next: top remaining (claude) task is Phase 9's last build item is now done; queue's next unchecked (claude) build task — none left in Phase 9; the ♻️ PERPETUAL TAIL (critique & replenish) is the next (claude) action once codex VERIFY tasks clear. Codex lane has VERIFY+STRESS backlog across all phases.

2026-06-27 21:31 UTC · Claude · files: NIGHT_QUEUE.md, AGENT_LOG.md · commit: e681326 · verification: full regression GREEN — `node --test public/lib.test.mjs` 79/79 pass, `node --check public/app.js` + `public/lib.js` + `public/i18n.js` all OK, `python3 -m py_compile pipeline/*.py` OK; GE15 tally invariant test green (PH82·PN74·BN30·GPS23·GRS6·WARISAN3 = 222). ♻️ CRITIQUE & REPLENISH round (perpetual tail — NOT ticked, left in place). All prior (claude) build lanes drained/ticked, so ran the audit per the regime. Read app.js + lib.js + index.html head + every lib.test name. APPENDED Phase 10 (4 new (claude) tasks + their codex VERIFY counterparts) above the perpetual-tail line. Findings: (1) REAL BUG (top priority) — Escape double-action: the `#q` Escape branch (app.js ~668) clears the search but does NOT stopPropagation, so the keydown bubbles to the document handler (~836) and ALSO deselects the seat/zooms out — one Escape, two actions. Repro: select a seat → type in #q → Escape clears search AND blows away the selection. Fix queued: `e.stopPropagation()` in the #q Escape branch only. (2) LATENT GUARDRAIL VIOLATION — `loadOptional()` (~124) calls `enableMode("skor")` whenever `data/scores.json` exists, which would un-gate the Skor tab and contradict the AGENTS.md UNTOUCHABLE "Keep Skor GATED even if scores.json appears". No live break (scores.json absent today) → queued as defensive hardening. (3) tested-foundation gap — `stateHues()` is the last pure helper still inline in app.js, untested → queued an extract-to-lib.js + tests task. (4) link-preview craft — `og:url`/`og:image:alt`/`og:locale` missing from `<head>` → queued (HVR). No code changed this round — replenish-only per the tail's contract. · next: codex VERIFY lanes, then the top (claude) fix = Escape double-action bug (Phase 10).

2026-06-27 (night) · Claude · files: public/app.js, NIGHT_QUEUE.md, AGENT_LOG.md · commit: d28c4b7 · verification: `node --check public/app.js` PASS; `node --test public/lib.test.mjs` => 79 pass / 0 fail. Phase 10 top fix — Escape double-action bug. The `#q` keydown Escape branch (~668) cleared the search (`Q.value=""; hideResults(); clearMatches()`) but did NOT stopPropagation, so the keydown bubbled to the document-level handler (~840 `if (e.key==="Escape" && state.selected) deselect()`) and ALSO deselected/zoomed-out the seat — one Escape, two actions. Repro: select a seat (zoomed) → focus #q + type → Escape clears search AND unexpectedly deselects. FIX: added `e.stopPropagation()` to the `#q` Escape branch ONLY (arrow/Enter branches untouched) + a 3-line comment citing the rationale. Escape inside the search now clears the dropdown/query without nuking the selection; a second Escape with focus outside #q still deselects via the global handler. [MV] machine-verified (syntax + full unit suite); the actual focus/keydown UX is Danial's morning call (no browser in sandbox). · next: codex VERIFY+STRESS Escape fix; then top (claude) task is Phase 10 HARDEN Skor gating (guardrail — stop `enableMode("skor")` un-gating on scores.json presence).

2026-06-27 (night) · Claude · files: public/app.js, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 34d3e9f · verification: `node --check public/app.js` PASS; `node --test public/lib.test.mjs` => 79 pass / 0 fail. Phase 10 [MV] HARDEN Skor gating (guardrail). `loadOptional()` (~124-127) called `enableMode("skor")` the moment a `data/scores.json` fetch succeeded — removing `disabled` + the title so the gated tab became clickable (and a `#parlimen/skor` deep-link would then activate it), directly contradicting the AGENTS.md UNTOUCHABLE "Keep Skor GATED ('Soon') even if scores.json appears". scores.json is absent today so nothing broke live — defensive. FIX: dropped the `enableMode("skor")` call (kept `state.scores = await s.json()` so the panel score row + `seatValueColor` skor branch still work) + added a 3-line comment citing the untouchable. `enableMode("parti")` left unchanged. Behavioural change (note): with a synthetic scores.json present the Skor tab now STAYS disabled with its "Soon/Segera" pill instead of un-gating. [MV] machine-verified (syntax + full unit suite green); the disabled-pill visual is Danial's morning call (no browser in sandbox). · next: codex VERIFY Skor stays gated (drop synthetic scores.json → tab stays disabled, #parlimen/skor doesn't activate, parti still enables); then top (claude) task is Phase 10 stateHues() extraction into lib.js + tests.

2026-06-27 (night) · Claude · files: public/lib.js, public/app.js, public/lib.test.mjs, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 419515a · verification: `node --check public/app.js` PASS; `node --check public/lib.js` PASS; `node --test public/lib.test.mjs` => 85 pass / 0 fail (was 79; +6 new stateHues cases). Phase 10 [MV] — extracted `stateHues(seats)`, the LAST pure helper still inline in app.js (~93-105), into a pure DOM-free export in lib.js. Faithful copy: same golden-angle 137.508° hue spacing, same `i%3` sat / `i%2` light jitter, same sorted-unique state order — app.js now `import { ... stateHues }` and the inline function body is replaced by a one-line pointer comment (still called at loadTier ~101 `json.hues = stateHues(json.seats)`, behaviour unchanged on real parlimen+DUN data). Hardened beyond the original: non-array seats → `{}` (`Array.isArray` guard, no throw); `seats.map(s => s && s.state)` so a null/missing-state entry folds to the `undefined` key instead of crashing (real data all have `.state` → byte-identical output). New lib.test cases: (1) deterministic + stable (same seats → byte-identical map, incl. real SEATS); (2) distinct states → distinct well-formed `hsl()` hues in sorted-unique key order; (3) golden-angle math pinned at i=0/1/2 (`hsl(0 38% 40%)` / `hsl(138 44% 45%)` / `hsl(275 50% 40%)`); (4) empty/null/undefined/string/number → `{}` no throw; (5) a seat with missing state doesn't crash, well-formed seat still coloured; (6) every real state resolves to a well-formed hsl on SEATS. [MV] machine-verified (syntax + full suite green); legend swatch + negeri-mode fill visuals are Danial's morning call (no browser in sandbox). · next: codex VERIFY+STRESS stateHues port (byte-identical to old inline hues on real parlimen+DUN, empty/garbage → {} no throw, stable across reloads); then top (claude) task is Phase 10 [HVR] link-preview completeness (og:url / og:image:alt / twitter:image:alt / og:locale).

2026-06-27 21:?? UTC · Claude · files: public/index.html, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 2a837dc · verification: `node --check public/app.js` PASS; grep confirms 4 new well-formed `<meta>` tags present, no duplicates, existing og/twitter/title/desc tags intact, `og:image` still → assets/og-card.svg (real file). Phase 10 link-preview completeness [HVR]. Added to `<head>`: `og:url` (placeholder `https://mypolitik.my/` — commented + flagged for Danial to confirm the real deployed domain), `og:locale=en_MY` (matches default EN copy), `og:image:alt` + `twitter:image:alt` (describe the share card). Markup-only, zero-dep, no build step. HVR note: rendered unfurl is Danial's morning eye — structurally the tags are valid but the actual og:url domain and a real 1200×630 og-card.svg→PNG still need his confirmation. · next: codex VERIFY link-preview additions (tags well-formed, no dupes, og:image resolves under public/, node --check). After that the only remaining unticked (claude) item is the ♻️ perpetual CRITIQUE & REPLENISH tail.

2026-06-27 22:30 UTC · Claude · files: NIGHT_QUEUE.md, AGENT_LOG.md · commit: c118481 · verification: FULL REGRESSION GREEN — `node --test public/lib.test.mjs` 85 pass / 0 fail; `node --check` clean on public/app.js, public/lib.js, public/i18n.js; `python3 -m py_compile pipeline/*.py` OK. ♻️ PERPETUAL TAIL (critique & replenish — NOT ticked, by design). All (claude) build tasks through Phase 10 are done; remaining unchecked items are (codex) verify lane. Ran the suite, then audited app.js + lib.js + index.html against the AGENTS.md vision. NO active bugs found — confirmed combobox a11y is complete (#q role=combobox + aria-autocomplete=list + aria-controls→#results which carries role=listbox; aria-expanded toggles), and BOTH link-preview assets resolve under public/assets/ (og-card.svg + icon.svg referenced by og:image/twitter:image/favicon). Appended Phase 11 (critique round 7) with 3 (claude) tasks + paired (codex) verifies ABOVE the perpetual-tail line: (1) [MV] HARDEN i18n-shadow latent landmine — the SVG mousemove (~551) and click (~576) handlers each `const t = e.target;`, shadowing the module-level `t()` i18n fn; harmless today (neither calls t()) but any future translated string inside throws "t is not a function" — rename locals to `tgt`; (2) [MV] remove dead `row()` helper (~258-260) — defined, never called (renderPanel builds rows inline); (3) [MV] tested-foundation — automate the markup-key parity check (read index.html via node:fs, extract every data-i18n* attr value, assert each exists in both I18N.en+ms) so the currently-manual "all markup keys resolve" check becomes a node --test guard. · next: codex picks up the round-7 VERIFY tasks, or claude takes the top Phase 11 (claude) task (i18n-shadow hardening).

2026-06-27 (night) · Claude · files: public/app.js, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 14928ad · verification: `node --check public/app.js` PASS; `node --test public/lib.test.mjs` => 85 pass / 0 fail; `grep -n "const t = e.target"` returns nothing (both removed). Phase 11 [MV] HARDEN i18n-shadow latent landmine. The SVG `mousemove` (~551) and `click` (~576) handlers each declared `const t = e.target`, which shadows the module-level i18n translation fn `t()` (app.js:56) inside those blocks. Safe today (neither block calls `t("...")`) but a trap — the moment anyone adds a translated string to the tooltip or click path, `t("key")` would throw `TypeError: t is not a function`. Renamed BOTH locals to `tgt` and updated their in-block uses (`t.classList`→`tgt.classList`, `t.dataset.code`→`tgt.dataset.code`) so the i18n fn is always reachable; added a one-line comment on each citing the reason. NO behaviour change (machine-verified — syntax + full unit suite green). · next: (codex) VERIFY i18n-shadow fix (both handlers reference `tgt`, tooltip+seat-click still resolve by dataset.code, no other `t` broke); then top (claude) task is Phase 11 — remove dead `row()` helper (app.js ~258-260).

2026-06-27 22:30 UTC · Claude · files: public/app.js, NIGHT_QUEUE.md · commit: 326369c · verification: `node --check public/app.js` PASS; `grep -n "row(" public/app.js` => no matches (orphan definition was the only one); `node --test public/lib.test.mjs` => 85 pass / 0 fail / 0 cancelled (regression GREEN, no logic touched). Phase 11 dead-code cleanup: deleted the unused `function row(dt, dd, mono=false)` (was app.js ~258-260) — renderPanel builds its `<dt>/<dd>` rows inline via template strings and `#summary`/legend build their own markup, so the helper had zero call sites. [MV] — pure removal, no behaviour change, shrinks the surface 3 lines. · next: codex VERIFY row() removal (confirm no orphan call site, renderPanel still emits the same `<dl>` rows, node --check), then the next (claude) task — automate the markup-key parity check (lib.test reads index.html, asserts every data-i18n* attr value exists in BOTH I18N.en + I18N.ms).

2026-06-27 (night) · Claude · files: public/lib.test.mjs, NIGHT_QUEUE.md, AGENT_LOG.md · commit: b583aed · verification: `node --test public/lib.test.mjs` => 86 pass / 0 fail (was 85; +1 markup-parity test); `node --check public/app.js` PASS; `node --check public/lib.js` PASS. Phase 11 tested-foundation: automated the markup-key parity check that was hand-verified each round. New test "I18N: every data-i18n* key in index.html exists in BOTH en and ms" reads public/index.html via readFileSync(new URL(...)) (same pattern as the existing data tests), regex-extracts every value of the six attribute flavours app.js's applyStatic() consumes (data-i18n, -ph, -title, -aria, -content, -after) — the `\b` after the attr name stops `data-i18n` from swallowing `data-i18n-ph` etc. — dedupes into a Set (34 unique keys today), and asserts each key is `in I18N.en` AND `in I18N.ms`. Manually confirmed the logic: all 34 keys resolve, a synthetic `data-i18n="bogus_nope"` is flagged, and the `data-i18n` vs `data-i18n-ph` disambiguation holds. Guards the silent-untranslated-markup regression class so a future markup edit referencing a missing/untranslated key fails `node --test` instead of needing a human grep. · next: codex VERIFY markup-key parity test (passes on current HTML; FAILS if a bogus data-i18n="nope" is added OR a referenced key is deleted from i18n.js en/ms). After that, Phase 11's (claude) build tasks are drained — next round runs the ♻️ CRITIQUE & REPLENISH tail.

2026-06-27 22:35 UTC · Claude · files: NIGHT_QUEUE.md, AGENT_LOG.md · commit: 464c585 · verification: full regression GREEN — `node --test public/lib.test.mjs` => 86 pass / 0 fail; `node --check public/app.js` PASS; `node --check public/lib.js` PASS; `node --check public/i18n.js` PASS; `python3 -m py_compile pipeline/*.py` PASS; GE15 tally-invariant test green; seats-parlimen.json count===seats.length===222 verified. ♻️ CRITIQUE & REPLENISH round 8 (all concrete (claude) tasks were ticked; only the perpetual tail remained doable). Audited app.js/lib.js/i18n.js/index.html against the AGENTS.md vision. Found ONE real keyboard-UX bug + 2 craft/foundation items; appended Phase 12 (3 new (claude) tasks + codex VERIFY pairs) ABOVE the perpetual tail. BUG (queued top of Phase 12): the `#q` Escape branch (app.js ~656-662) ALWAYS calls `e.stopPropagation()`, even when the query is empty AND the dropdown is hidden — so a seat-selected user who clicks into the empty search box and presses Escape gets an inert no-op (search clears nothing, the document-level Escape→deselect at ~831 is blocked). Keyboard deselect dead-ends while focus is in #q. Fix proposed: early-return `if (Q.value === "" && RESULTS.hidden) return;` so a no-op Escape bubbles to deselect, while a non-empty query/open dropdown still clears search-only (preserves the Phase-10 one-Escape-one-action fix). Other two: [MV] extract the pure zoom-framing math from `zoomToSeat` (~212-223) into a tested `seatViewBox(bbox, full)` in lib.js (last meaningful pure logic still inline; regime: every pure fn ships WITH tests); [MV] defensive `renderSummary` fallback `data.count ?? data.seats.length` so a future data file missing `count` never shows "undefined". No code touched this round (audit + replenish only). next: a (claude) agent takes the top Phase-12 bug fix (Escape-bubble); codex VERIFY pairs follow each.

2026-07-02 17:45 UTC · Claude · files: public/app.js, public/styles.css · commit: (uncommitted, on top of 97bdadc) · MOBILE POLISH WAVE — verified the CLAUDE_HANDOFF.md flow end-to-end at 390×844 via headless-Chrome CDP driver (scratchpad mp-drive.js): Sarawak → P.195 preview → More → detail; redundant state header display:none PASS, no .where state line PASS, district picker pinned (gap 17px constant under scroll) PASS, back-to-inspect widthRatio 1.000 PASS, zero page exceptions. BUG FOUND+FIXED: SVG pointerdown in map-inspect called setPointerCapture immediately → pointer capture retargeted mouseup/click to <svg> → MOUSE taps on districts read as empty-space → goBack closed the state (touch was unaffected — capture releases before touch compat clicks; verified via Input.dispatchTouchEvent). Fix: capture deferred to first real drag movement (>5px) in pointermove. ALSO FIXED: --map-h measured against the scaleY(0.08)-held card during swapCardWithMinimizePop (map band ~560px behind a 264px-top card) → re-sync after transforms restore; #state-label clamped into the visible topbar at deep zooms → floor = topbar bottom + 6 when topbar visible. NEW (micro-animation pass, all compositor-only + reduced-motion paths): press-pulse on the map itself (tapped state dips in overview, tapped district when isolated); mobile seat-detail now FRAMES THE SELECTED DISTRICT in the map band (zoomToDistrict in showDistrict + More path — was a meaningless half-cut state sliver); tray YB overview is now a real <button> (#map-inspect-details, 466×72 target) opening detail with the same minimize-pop as More; tray overview rises in only when the selection CHANGES; new tab's panel rises in on seat-tab switch; toast slide actually plays (reflow before .show); pop-down/pop-up entrances for burger menu, info card, search results, district picker list; caret rotation transition; selected-district texture fades in. ALIGNMENT: .card-back 48→44 (matches 44px icon row), ≤380px district row unified at 44px (select/More were 46 beside a 44 fab). verification: node --check PASS; lib tests 48/48 dots PASS; scripts/validate.sh 4/4 PASS; layout-prop transition grep CLEAN; audit_ui.js PASS (12 elements, hit targets ≥44, all named); headless flows re-run PASS on mouse (capture fix proven — district click targets path.P.195 first attempt), touch, reduced-motion (flow completes, gaps identical), and desktop 1440×900 (Kepong detail opens, label below topbar). NOTE: earlier desktop driver miss was a DRIVER artifact (CDP integer-rounded click coords vs sub-pixel edge point), not an app bug. · next: Danial eyeball on a real phone (feel of press-pulse/pop timings is taste); consider picker-list exit animation + label overlap on small desktop states (pre-existing) if he wants another pass; then staging deploy per README when asked.

2026-07-02 18:20 UTC · Claude · files: public/app.js · commit: (uncommitted) · COORDINATED MAP+CARD MOVE (Danial: "state map should shrink smoothly as the card pops upward"). Previously the district zoom ran AFTER the minimize-pop finished (zoom-after-pop, two moves). Now: (1) syncMapToCard measures the card's LAYOUT top (panel bottom − padding − offsetHeight) instead of its transformed rect, so the map band is final-size even while the card is mid-animation (kills the band snap); (2) swapCardWithMinimizePop grew an onLayout hook fired as the map FLIP starts; showMapInspectDetails launches zoomToDistrict(seat, 560) there, so viewBox shrink + band FLIP + card rise run CONCURRENTLY and settle together; (3) refitOpenStateMap now refits to the CURRENT view target — selected district when mobile seat-detail, state otherwise — because setMapInspect(false)'s rAF refit was cancelling the concurrent district zoom (frame-sequence capture caught it: detail ended at state framing, sel district 2px). Bonus: rotation/resize while in mobile detail now re-frames the district, not the state. verification: node --check PASS; lib tests PASS; validate.sh 4/4 PASS; full mobile flow re-run PASS (district centered in band 154–176 of 78–264, back widthRatio 1.000, no exceptions); SEQ frame capture (8×~90ms through the pop) shows card mid-rise WITH map mid-shrink converging together. · next: Danial re-feel on phone; tune 560ms if the shrink should linger/snap more.

2026-07-04 · Claude (laptop) · files: (commit only) · commit: 3e74875 + this log · STAGING DEPLOYED. Committed the full 2026-07-01→03 mobile map-first wave (19 files, +3653/−518: state-inspect flow, pointer-capture fix, coordinated map+card motion, micro-animation pass, pipeline/04_verified_content.py + GE15/DUN candidates + voting-guide JSONs, CLAUDE_HANDOFF.md; scratchpad-sweep.js left untracked as a local tool). Pre-commit verification: node --check PASS, lib tests 48/48, scripts/validate.sh 4/4. Deployed `npx wrangler deploy --env staging` → mypolitik-staging version 3cd377ac, live at staging.mypolitik.krackeddevs.com (index 200, data/candidates-ge15.json + voting-guide.json 200, deployed app.js contains refitOpenStateMapSettled). · next: Danial phone eyeball on staging; prod deploy (mypolitik.xyz) only after his approval per staging-first rule.

2026-07-04 · Claude (laptop) · files: public/app.js · commit: ca06f61 · MORE-POP SYNC (Danial: "card should expand upwards a little slower; map should shrink in sync — right now they move at their own speeds"). Root causes: (1) three different clocks — band FLIP 360ms / card enter 420ms / viewBox zoom 560ms; (2) the enter's 58%-offset overshoot keyframe front-loaded the rise: with the decelerate easing, eased progress hits 0.58 at ~17% of the duration, so the card was FULLY risen ~100ms in and spent the rest settling 1.035→1 while the map still glided (measured: card motion ended ~805 vs band 903 / zoom 920). Fix: shared `DETAIL_POP_MS = 600` + `DETAIL_POP_EASE = cubic-bezier(0,0,0.2,1)` for band FLIP, viewBox zoom and card enter (animateRectFlip grew an easing param); enter is now a SINGLE-SEGMENT rise (overshoot dropped, opacity full by offset 0.35) so card scaleY and map FLIP follow the identical progress curve. Minimize exit stays 220ms. verification: headless CDP driver (scratchpad mp-sync-drive.js, raw-WS + rAF sampler; Sarawak P.215 tap → P.195 tap → #map-inspect-more click): START spread rise/band/zoom 38ms, END spread 34ms (was 116ms pre-fix), band 579ms / zoom 596ms, panel lands in seat-detail; node --check, 48 lib tests, validate.sh 4/4 PASS. DEPLOYED staging → version 6ff00db2, DETAIL_POP_MS confirmed in served app.js. · next: Danial phone re-feel; if the pop should keep a hint of bounce, re-add a LATE subtle overshoot (offset ~0.9, scaleY ~1.01) — it will still track ~90% of the way.

2026-07-04 · Claude (laptop) · files: public/app.js · commit: 20e82c1 · SHRINK SMOOTHNESS + DETAIL FRAMING (Danial: "state shrinking can be smoother; make the shrunken state a little bigger; center it between the state title and the top of the card"). ROOT CAUSE of the roughness: the shrink was TWO composited systems — the band's element FLIP applied a NON-UNIFORM scaleY (width constant, height shrinking) so the state geometry visibly stretched mid-move, on top of the viewBox glide, PLUS setMapInspect(false)'s refit restarted the glide one frame in. Now ONE camera: new `setViewBoxPreservingScreen(oldRect,newRect)` rewrites the viewBox after the band's layout snap so content renders pixel-identical (same scale k=min(w/vw,h/vh), same screen position), then `zoomToDistrict(seat, DETAIL_POP_MS, DETAIL_POP_EASE_FN)` glides — `cubicBezierEase(0,0,0.2,1)` (bisection solver) = the EXACT curve of the card's WAAPI rise. swapCardWithMinimizePop's onLayout now receives (firstMap,lastMap) and returning true skips the SVG FLIP; refitOpenStateMap skips zooms while mapInspectDetailsAnimating (tap-mid-pop worst case: back-zoom starts at the 360ms settled refit). BIGGER: zoomToDistrict pad 0.85→0.62 (~20% larger). CENTERED: mobile seat-detail pins #state-label under the topbar (syncStageLabelPosition early-exit, also cheaper per-frame) and syncMapToCard starts the band below the label (+6+labelH+8), so xMidYMid centers the map between title and card. verification (mp-sync-drive.js extended): START/END spread 51/50ms; max single-frame seat step 4.4% of total (continuous, preserve-step proven); district ASPECT deviation 0.00% across the glide (distortion-free proven); settled gaps title→band 8px, band→card 12px; screenshot eyeballed; BACK-from-detail regression: viewBox width ratio 1.0000; node --check, 48 lib tests, validate.sh 4/4 PASS. DEPLOYED staging v cba97e28. · next: Danial phone re-feel (esp. whether 0.62 pad = "a little bigger" enough).

2026-07-04 · Claude (laptop) · files: public/app.js · commit: a7dbe80 · WHOLE-STATE DETAIL FRAMING (Danial: "top of the state cuts off when shrunk; do not want anything cut"). The district-framed camera (zoomToDistrict) inherently crops the state, so mobile seat-detail now frames the WHOLE state fitted to the band's aspect (stateViewBox, 6% pad), selected district highlighted not zoomed-to: More-pop onLayout glide → zoomToState(openState, DETAIL_POP_MS, DETAIL_POP_EASE_FN); showDistrict mobile branch + refitOpenStateMap → zoomToState (seat-detail special-case collapsed); locate→details dropped its extra zoomToDistrict (showDistrict handles); zoomToState grew (ms, ease) params; zoomToDistrict pad reverted 0.62→0.85 (only inspect-mode locate preview uses it now). Nice side-effect: switching districts via the detail dropdown no longer moves the camera (state framing is invariant). verification (driver now probes the .instate UNION bbox — P.195 alone is ~3px at state framing, too small a probe): whole state inside band, 0px crop, 15.7px pad top/bottom; title→band 8px, band→card 12px (centered); glide continuous max frame step 13.3%, aspect dev 0.00%, START/END spread 34/67ms; back-to-inspect ratio 1.0000; screenshot eyeballed (full Sarawak shape centered under title). node --check, 48 lib tests, validate.sh 4/4 PASS. DEPLOYED staging v 2e95a491. · next: Danial phone re-feel.

2026-07-04 · Claude (laptop) · files: public/app.js, public/styles.css · commit: e6fde66 · SCREEN-SPACE SELECTED-DISTRICT STRIPES (Danial screenshot of W.P. KL: "lines when I click a district is buggy, thick and not the same — make them always visible, thin and many across all districts"). ROOT CAUSE: `#selected-district-lines` pattern was patternUnits=userSpaceOnUse w/ 4.5-UNIT pitch + 1.15-unit stroke in the FROZEN projection — px-per-unit varies ~20× (isolated W.P. ~40px/unit vs Sarawak ~2px/unit), so KL rendered ~180px pitch / ~46px lines = solid pale blob; big states looked fine. FIX: `syncSelectedTexture()` re-derives pattern width/height/line-d/stroke-width from the CURRENT meet scale k=min(rect.w/vbW, rect.h/vbH) → constant 5px pitch / 1px stroke ON SCREEN at every zoom; called per camera frame (animateTo step + reduced-motion jump branch, setViewBoxNow), on band resize (syncMapToCard rAF), and on selection (setSelectedDistrict); early-outs when no overlay. Plus THINNED the selected outline: `#seats.isolated .seat.sel` 3/3.2→1.8px, `.selected-texture` stroke 1.4/1.6→1.1px (all vector-effect non-scaling already). verification: new scratchpad driver mp-texture-check.js (gotcha: same-hash Page.navigate doesn't reload — bounce via about:blank) measured stripe pitch/thickness at KL P.114 + Sarawak P.201 in BOTH inspect and detail views: 5.00px/1.00px in all 4; screenshots eyeballed (KL Kepong = clean thin hatching, was blob). node --check, 48 lib tests, validate.sh 4/4 PASS. DEPLOYED staging v febfde1f. · next: Danial phone re-check on KL + a big state.

2026-07-04 · Claude (laptop) · files: public/app.js · commit: 0d8fb4f · POP CHOREOGRAPHY: CARD LEADS, STATE FOLLOWS (Danial: "let the card pop down first; when it pops upward, only then let the state shrink" — supersedes the round-1 "in sync" concurrency). onLayout still preserves the state pixel-identical at the band snap, but the glide now starts on a 200ms setTimeout after the rise begins (`DETAIL_GLIDE_DELAY_MS=200`) and runs 430ms (`DETAIL_GLIDE_MS`) so it settles just after the card. Stacking already correct: #panel z-30 over #stage z-1, so the card rises OVER the still-big state and the state then shrinks up into the band behind it. Guards: back-tap within the delay cancels (PANEL.seat-detail check in the timer); reduced-motion/anim-off path (onLayout called with no rects) jumps straight. verification: driver rise-detection reworked (direction flip of cardTop — the pop-down/hold/pop-up gap is only 1 frame); measured pop-down 217ms → rise 277 → glide 527 (lag 250ms) → card settles 877 / glide 960 (spread 83ms); continuity max step 18.8%, aspect dev 0.00%, whole state in band, back ratio 1.0000; node --check, 48 lib tests, validate.sh 4/4 PASS. DEPLOYED staging v 4c3b9b63. · next: Danial re-feel; dials = DETAIL_GLIDE_DELAY_MS (lag) + DETAIL_GLIDE_MS (shrink speed).

2026-07-04 · Claude (laptop) · files: public/styles.css · commit: 1f1757e · HOLD-CLIP FIX (Danial: "when the card collapses, before going back up — a line/box covering the bottom part of the state"). Diagnosed via burst frame-capture (scratchpad mp-hold-frames.js, 8 shots through the pop): at the swap the band layout snaps small while the state holds full-size (the card-leads choreography makes that window ~450ms visible), and the SVG root's default overflow:hidden CLIPPED everything below the new band bottom — a hard horizontal slice through the state (screenshot showed only Sarawak's top arm fragment). FIX: `body.state-open #map { overflow: visible }` — while isolated, the map may paint outside its band box; safe because outstate seats are opacity 0 (only the isolated state can spill) and the rising card (z-30 over stage z-1) covers it from below. Deliberately NOT global: desktop/overview deep zooms would spill all-Malaysia geometry outside the band. verification: fresh hold frames show the FULL uncropped state standing still w/ card popping over it; full regression driver PASS (lag 233ms, end spread 83ms, max step 21.1%, aspect 0.00%, settled no-crop, back ratio 1.0000); validate.sh 4/4 + 48 lib tests. DEPLOYED staging v d1cbf502. ⚠️ frame-capture gotcha: screenshot filenames carry wall-clock ms — check ls timestamps, an earlier run's near-identical names can masquerade as fresh. · next: Danial phone re-feel of the full pop.

2026-07-04 · Claude (laptop) · files: (deploy only) · commit: b10872a (tip) · 🚀 PROD DEPLOYED (Danial: "push to main" = ship prod; staging-first approval given after phone review). `npx wrangler deploy` → worker `mypolitik` version ff061137, FIRST prod deploy — provisioned custom domains mypolitik.xyz + www.mypolitik.xyz + mypolitik.krackeddevs.com (www answered 301 immediately; apex + krackeddevs took the usual first-deploy DNS/SSL provisioning minutes — background curl-watch confirms 200s). Prod now serves the full 2026-07-04 wave: whole-state detail framing, card-leads pop choreography, distortion-free camera glide, screen-space stripes, hold-clip fix, atop the committed mobile polish wave (3e74875). NOTE: repo has NO git remote — "push to main" on this project = wrangler prod deploy from the laptop. · next: aspect-ratio compact-mode work (analyzed, awaiting go).

2026-07-04 · Claude (laptop) · files: pipeline/prn16_johor_candidates.csv, pipeline/05_prn16_johor.py, public/data/prn16-johor.json, public/app.js, public/lib.js, public/lib.test.mjs, public/i18n.js, public/index.html, public/styles.css, worker.js · commit: 3af791b · 🗳️ PRN16 JOHOR LIVE-ELECTION MODE (campaign phase), staging v d81bd38b. DATA: 172 SPR-confirmed candidates / 56 DUN seats TRIPLE-CROSS-CHECKED — Sinar Harian full list + Wikipedia BM both independently hit the official totals AND contest shape (14×2-way/27×3-way/12×4-way/3×5-way); Wikipedia EN was PARTIAL (table literally ends mid-row at N56) and mislabeled BERSAMA candidates as PN — outvoted 2-1. Adjudications: N.17 Semerah PH=PKR (Sinar+EN vs MS AMANAH), N.48 Skudai MUDA-PSM=PSM (Sinar specificity). Gotchas: Sinar coalition labels are Title-case ("Bersama","Muda") + "Bebas-Kereta Api" = ballot SYMBOL for independents; MS-wiki table = 3 cols/coalition (incl. "nama pada kertas undi" ballot alias). 05 script HARD-ASSERTS all official counts. UI: overview badge (camera-tracked via viewBox→screen math, synced in animateTo/setViewBoxNow/syncMapToCard alongside label+texture; ⚠️ `.live-badge[hidden]{display:none}` needed — explicit display defeats the hidden attr); banner+button in stateSummaryHTML + mobile tray row; PRN view = DUN tier + Johor isolated + #39404c neutral seats (seatValueColor branch reads state.prnLive for polling night); prnSeatCardHTML candidate cards (coalition pills: PRN_COLORS adds BERSAMA/MUDA-PSM/OTHERS; ballot-alias noise filtered via bin/binti-normalized nameKey); prnSummaryHTML (countdown/dates/contested sharebar/majority/MySPR/source/exit); hash `/prn` token (lib.js encode/decode + tests, old shape-tests extended w/ prn:false); i18n EN+BM. Exits: prn-close btn, manual tier switch, backToControls (all silent-close). LIVE SEAM: worker /api/live/johor → KV PRN_LIVE key "johor" w/ campaign fallback (verified on staging: {"phase":"campaign"}); refreshPrnLive() polls + repaints when phase flips. Verified headless 390×844 + 1280×900: badge→mode(hash #dun/parti/prn, grey fill, Johor open)→candidate card (N.33 Tenggaroh, 3 cands) PASS both, screenshots eyeballed. 90 lib tests, validate.sh 4/4. · next: Danial phone check → prod; then polling-night poller (KV write + source recon 10-11 Jul); N9 1 Aug = new config JSON.

2026-07-04 · Claude (laptop) · files: public/index.html, public/styles.css, public/app.js · commit: 4e3fa81 · IPHONE APP-FEEL FIXES, staging v 7f1dd2f1 (Danial: search bar vanishes behind the keyboard + page "weirdly zooms in" on search focus; wants no zoom on mobile — app feel). THREE root causes: (1) mobile `.search input` was 14px — iOS Safari AUTO-ZOOMS the page when a focused input is <16px → now 16px (kills it at the source); (2) viewport meta allowed scaling → now `maximum-scale=1, user-scalable=no, viewport-fit=cover, interactive-widget=resizes-content` + `touch-action: pan-x pan-y` on html/body/#stage/#map (⚠️ touch-action does NOT inherit) + `gesturestart` preventDefault (Safari-only event, blocks pinch page-zoom); (3) the on-screen keyboard shrinks the VISUAL viewport while position:fixed tracks the LAYOUT viewport → bottom card sank behind the keyboard + Safari scroll-jumped the page hunting for the input → `syncKeyboardInset()` on visualViewport resize+scroll maintains `--kb-inset` = innerHeight − vv.height − vv.offsetTop; `#panel { bottom: var(--kb-inset) }` lifts the card above the keyboard, page pinned via scrollTo(0,0), syncMapToCard re-syncs the band. verification: headless — search flow intact, input computes 16px, --kb-inset:300px lifts panel exactly 300px; PRN drive re-run PASS mobile+desktop; 90 lib tests, validate.sh 4/4. (True keyboard feel needs the real iPhone — vv shrink not emulatable headless.) · next: Danial re-test search on iPhone; both PRN mode + these fixes ready to promote to prod together.

2026-07-04 · Claude (laptop) · files: public/index.html, public/styles.css, public/app.js · commit: 1c49f1f · HOME LOGO AT ALL TIMES, staging v ecc5b2d9 (Danial: MyPolitik logo always on screen, clickable → main page). Gap: `body.map-inspect #topbar { opacity:0 }` hides the whole topbar (incl. the existing #brand-home → showWholeMap button) in the isolated-state view — no logo, no home affordance there. Fix: `#inspect-home` — compact "◧ MyPolitik" pill (reuses .brand-home styling) FIXED top-right (the hidden burger's corner), display:flex only under `body.map-inspect` (mobile MQ), click → showWholeMap (hideInfo + backToControls, which also silent-exits PRN mode). syncStageLabelPosition's floor now also clears the pill when topbar hidden (wide state names like W.P. Kuala Lumpur would collide top-right; probe visibility via getClientRects().length — offsetParent is null for position:fixed). verification: headless 390×844 — hidden on overview ✓, visible 14px/14px from top-right in inspect ✓, click → state closed + topbar restored + chip gone ✓, Sarawak label clears it ✓, screenshot eyeballed; PRN drive re-run PASS; 90 lib tests + validate.sh 4/4. · next: Danial phone check; staging now carries PRN mode + iPhone app-feel + home logo, all ready to promote together.

2026-07-04 · Claude (laptop) · files: public/app.js, public/styles.css · commit: 9d9b093 · KEYBOARD SEARCH ROUND 2, staging v 70eb6d3f (Danial iPhone: still hidden — card SHRINKS and covers the search bar; his diagnosis "map not meant to shrink further / hide the stats while typing" was correct on both counts). The round-1 --kb-inset lift was necessary but insufficient: (1) syncMapToCard re-derives --panel-card-max-h from the SHRUNKEN visual viewport while minMapH held 144px → card starved (~200px); (2) #nat-glance (+#empty-more) sit ABOVE .search-row inside #panel-empty → search fell below the card's internal fold. FIX: `keyboardInset` module var (set in syncKeyboardInset) → syncMapToCard zeroes minMapH/viewportBoundMin while keyboardInset>60 (map yields everything to the card); `body.searching` on #q focus hides #nat-glance+#empty-more (mobile MQ) → card = search row only; blur removes it after 250ms (result taps land before re-layout; focus also re-pins scroll at +350ms). verification: emulated keyboard-height viewport 390×430 with real focus: searching✓ statsHidden✓ input fully visible (356-405 of 430) zero card scroll✓; blur restores stats✓; screenshot = card is just the search bar w/ map+PRN badge above; PRN drive PASS; 90 tests + validate 4/4. NOTE: headless can't truly emulate iOS vv-vs-layout viewport split — if the phone STILL misbehaves, ask Danial for a screen recording (suspect Safari asset cache first: hard-refresh). · next: Danial iPhone re-test.

2026-07-04 · Claude (laptop) · files: public/app.js, public/styles.css · commit: (see git log tip) · KEYBOARD ROUND 3 (Danial iPhone: card still UNDER the keyboard despite correct inset). ROOT CAUSE: iOS FREEZES position:fixed anchoring while the keyboard is open — the `#panel { bottom: var(--kb-inset) }` offset computed right but was visually IGNORED. Fix: syncKeyboardInset applies `PANEL.style.transform = translateY(-keyboardInset)` (compositor transforms are NOT frozen — the bottom-sheet-library pattern); CSS bottom rule removed (single lift mechanism, no double-shift); cleared at kb=0; syncMapToCard measures the TRANSFORMED panel rect so the band math adapts free. ⚠️ LESSON for all mobile bottom-sheets: on iOS, keyboard lift = transform, never bottom/top offsets. Regressions PASS (kb-viewport search, PRN flow, 90 tests, validate 4/4). Staging deployed. · next: Danial iPhone re-test (hard-refresh).

2026-07-04 · Claude (laptop) · files: public/app.js · commit: a2c5d36 · KEYBOARD ROUND 4, staging v 60faec90. Danial's screenshot proved the lift never EXECUTES on his iPhone (map centered against a full-height layout, card parked under the keyboard) → suspicion: iOS visualViewport resize/scroll events not firing (or firing mid-animation) on his version, so neither the round-2 var nor the round-3 transform ever ran with kb>0. FIX: while #q is focused, a rAF FOLLOW-LOOP reads the visual viewport every frame (no event trust) — recomputes kb, full syncKeyboardInset only when kb CHANGES, otherwise just re-pins the translateY + scroll origin; stops when focus leaves; blur does a final sync +700ms belt-and-braces. PLUS on-device diagnosis: `?kbdebug` query param → live HUD (innerHeight/vvH/vvTop/kb/scrollY/transform). If his phone STILL fails: have him open staging.mypolitik.krackeddevs.com/?kbdebug#parlimen/parti, focus search, screenshot — the numbers will name the liar (ih==vvH ⇒ layout resizes on his iOS ⇒ different fix: drop the transform, trust fixed-bottom). Regressions PASS (kb-viewport, PRN, 90 tests). · next: Danial re-test (PRIVATE TAB to dodge Safari cache), else kbdebug screenshot.

2026-07-04 · Claude (laptop) · files: public/app.js, public/i18n.js, public/styles.css · commit: effd203 · DUN DRILL CONTEXT (first external user feedback: "no indicator of what the constituency or district is at DUN level"), staging v fa72a97d. Three additions, zero new data: (1) big state label carries a `.label-tier` chip (DUN/Parliament) whenever a state is open — derived INSIDE setStageLabel from state.openState+state.tier (memo key includes the chip) so hover-restore paths keep it; (2) DUN seat cards: "Parliament P.214 · Selangau" where-line via new `parlimenContext(seat)` (resolves name from state.data.parlimen — already cached in DUN tier by the FT underlays; degrades to bare code); (3) same line on the PRN candidate card. i18n `parlimen_label` EN/BM. verification: headless — chip "DUN" on isolated Sarawak, N.60 Kakus shows P.214 · Selangau, PRN N.33 shows P.154 · Mersing (that run ALSO proved the `#dun/parti/1_N.33/prn` deep link end-to-end); PRN flow PASS; 90 tests + validate 4/4. Screenshot eyeballed (Sarawak · DUN chip + context line, clean). QUEUED (user suggestion #2, next session after the PRN poller): MB/KM per state + DOSM state-economy report card (GDP/growth/unemployment via data.gov.my) on the state summary; economy map mode later. · next: keyboard fix CONFIRMED WORKING by Danial (private tab — Safari cache was the masker all along).

2026-07-04 · Claude (laptop) · files: public/index.html, public/styles.css, public/app.js · commit: 9a0371e · TOPBAR AT ALL TIMES, staging v 7538a06a (Danial: "MyPolitik stays top-LEFT with the menu bar top-right, always" — SUPERSEDES today's earlier #inspect-home top-right chip, now fully removed: element+CSS+listener). `body.map-inspect #topbar` hide rule DELETED — brand pill (→ showWholeMap) + burger visible in the isolated-state view too; syncMapToCard: chromeBottom always topbar-based, band top = chromeBottom + (inspecting ? 8 : 12) — the isolated state gives up ~70px of height for permanent chrome; syncStageLabelPosition floor = simply topbar bottom + 6 (chip clause gone). verification: headless 390×844 — brand 10px from left + burger 10px from right in BOTH overview and inspect; label below the bar; brand click → home; screenshot eyeballed ("Sarawak · PARLIAMENT" chip below chrome, clean); PRN drive PASS, kb-search PASS, 90 tests, validate 4/4. · next: Danial phone check (private tab); staging bundle = PRN mode + iPhone fixes + DUN context + always-topbar.

2026-07-04 · Claude (laptop) · files: pipeline/{06_candidate_news,07_state_context,08_state_econ}.py, pipeline/live/*, public/data/{state-context,state-econ,live-johor}.json, public/app.js, public/i18n.js, public/styles.css, worker.js · commits: 73f034e + c50a032 · 🏗️ FOUR-WORKSTREAM WAVE ("lets do all of it now"), staging deployed. (1) POLLING-NIGHT POLLER: pipeline/live/johor_poller.py (pluggable sources; manual CSV = guaranteed path; single non-manual source can only publish "leading", win = 2 sources OR manual; append-only provenance log; publishes live-johor.json + optional auto-deploy ~15s). KV blocked (deploy token lacks KV perms, auth 10000) → worker /api/live/johor: KV → ASSET live-johor.json → campaign; DRESS-REHEARSED with a 13-won/13-leading/30-counting fixture: LIVE chip + "Declared 13 of 56" + tally bar w/ majority line + map paints solid/45%-translucent/grey — verified headless + screenshot; race fix: live summary re-asserts 800ms after the reveal choreography. Reset to campaign. NIGHT-OF TODO: 17:00 recon fills THESTAR_URL; run `--watch 90 --phase live --deploy prod`. (2) ELECTION COUNTDOWN: 14 legislatures researched w/ agent fan-out (all first-sittings verified; Sabah 2025 election → 2030 clock; Sarawak due 14 Feb 2027 "expected 2026"; Melaka expires 27 Dec 2026) → pipeline/07 bakes state-context.json → card row "Next election: due by <dissolve+60d> · in N days" (announced dates exact; FTs = GE16 clock; suppressed when PRN banner owns the state). (3) MB/KM: web-verified incl. PERLIS CHANGE (Abu Bakar Hamzah, BERSATU, Dec 2025 — training data would say Shukri!) + Sabah Hajiji re-mandate; caretakers flagged (Johor, N9). (4) ECONOMY: DOSM via api.data.gov.my (agent live-tested: gdp_state_real_supply growth_yoy+abs @p0, population_state for derived REAL per-capita, lfs_qtr_state u_rate 2025-Q3, hies_state median income 2022; gotchas: ONE comma-joined filter param, follow 301s, Putrajaya folded into KL for GDP, GDP stuck at 2023) → pipeline/08 bakes state-econ.json; national growth computed 3.55% ✓ cross-checks DOSM ~3.6%; card rows GDP-growth-vs-national ▲/▼, per-capita, unemployment, median income + CC BY 4.0 source line. (5) CANDIDATE NEWS: pipeline/06 GDELT DOC API (open license; Google News RSS rejected on ToS) — ⚠️ GDELT 429s at <5s spacing → THROTTLE 5.5s + 25s backoff ×3; bake running (~16 min); UI seam live on PRN cards (linked headlines only, per-candidate, namesake disclaimer). Sarawak card verified: Premier + countdown + 4 econ KPIs + source. "Show support" sentiment feature REJECTED (bots + neutrality risk) — issue-based polling maybe post-PRN. · next: news bake lands → verify news UI → staging; Danial promote-all.

2026-07-04 · Claude (laptop) · files: pipeline/06_candidate_news.py, public/data/candidate-news-johor.json · commit: (git log tip) · NEWS BAKE SAGA: GDELT TEMP-BANNED the laptop IP after the first 1.2s-spaced run (429 on ALL requests incl. curl probes; 5.5s throttle irrelevant once banned) AND this session kills long background jobs (4 killed) → bake made fully kill-proof: checkpoint every 10 candidates + `_done` ledger (⚠️ without recording no-hit candidates, resume re-fetched the misses forever — progress stalled at 2). UI verified end-to-end with the partial data (N.01 detail shows "In the news": 2 real headlines + GDELT disclaimer). VPS-run route DENIED by permission classifier (own scope escalation — Danial must authorize). REMAINING: 170 candidates — rerun `python3 pipeline/06_candidate_news.py` when the IP cools (resumes automatically) or from the VPS with Danial's blessing; file is additive, UI degrades gracefully. · next: Danial decides bake route + promote-all to prod.

2026-07-04 · Claude (laptop) · files: public/index.html, public/styles.css, public/i18n.js, public/assets/kd-logo.png · commit: (git tip) · INFO-CARD CREDIT + ALIGNMENT LINE (Danial: credit footer + KD logo; "alignment is key" — info box sides must match topbar buttons + bottom card). MEASURED first: info 21/14, card 12/12, buttons 10/10 = three different lines → unified on 12px: mobile #topbar padding 10→12, #info-card left/right 12 (width auto ≤860). Footer: i18n `info_credit` EN/BM + kd-logo.png (sips-resized 1640→256px, image-rendering:pixelated, → krackeddevs.com). Headless-verified EXACT: brand.l=burger.rIn=card=info=12; screenshot eyeballed. Staging deployed. · next: Danial eyeball; promote-all still pending.

2026-07-04 · Claude (laptop) · files: public/app.js, public/i18n.js, public/styles.css · commit: bb29b38 · DESKTOP DISTRICT FINDER (Danial: outside mobile mode, tapping a state should still give the district dropdown). The picker only lived in the mobile inspect tray; desktop had map-tap only. Fix: stateSummaryHTML + prnSummaryHTML render a "Find a district" (i18n `find_district` EN/BM) dropdown+locate via districtSwitchRowHTML when `!MOBILE_MAP_INSPECT_MQ.matches` (mobile tray keeps its own → no dup); selection reuses the existing PANEL_STATE handler → showDistrict on desktop; list opens DOWNWARD (base .map-inspect-select-list style; only mobile flips it up). **LATENT BUG FIXED: openStateCard set state.openState AFTER `STATE_INFO.innerHTML = stateSummaryHTML(name)`, so districtOptionsForOpenState() returned [] → dropdown empty on first open; moved openState/selected assignment BEFORE the render.** verification: headless 1280px — finder renders on state tap (31 Sarawak parlimen / 56 Johor DUN options), `.click()` opens list + selects "Bandar Kuching" → seat-detail + 1 highlighted path; works in PRN mode; screenshot eyeballed. ⚠️ CDP coordinate-click on the toggle was flaky (integer-rounded coords vs button edge — known artifact); `.click()` + real taps fine. 90 tests, validate 4/4, PRN+ctx regressions PASS. Staging e900a5ee. · next: Danial eyeball; promote-all pending; news bake still 2/172 (GDELT IP cooldown).

2026-07-04 · Claude (laptop) · files: public/app.js, public/styles.css · commit: 7ec01dc · DESKTOP CARD FIXES (Danial screenshots: tapping a state on desktop squashes the state SVG into a sliver + no visible district selector). ONE root cause: the content-rich card (stats+economy+context) grows tall → starves the map band (fixed 160px desktop min) → AND pushes the finder BELOW the card's scroll fold (finder WAS present, just at the bottom past the economy table — measured cardScrolls 127 at 940px). Fixes: (1) desktop/landscape `preferredMinMapH` 160px → `floor(viewportH*0.40)` — map 160→329px at 940×900 (mobile inspect + kb paths untouched); (2) moved the district finder to the TOP of both the state summary card AND the PRN summary card (right under the header, with a `.state-district-find-top` divider) — it's the primary action, now visible without scrolling. NOTE: Danial's no-finder screenshot PREDATED the finder deploy (his had economy c50a032 but not finder bb29b38) → hard refresh shows it; the 1280px finder=false in my measurement was a missed-tap artifact (opened overview not state). verification: 940px map 329px + finder at card-top fully visible (screenshot eyeballed); PRN+sync+ctx regressions PASS, 90 tests, validate 4/4. Staging 96e478e5. · next: Danial hard-refresh + eyeball; promote-all pending; news bake 2/172 (GDELT cooldown).

2026-07-04 · Claude (laptop) · files: public/app.js, public/styles.css · commit: 49af85d · DESKTOP: PERSISTENT SEAT-DETAIL FINDER + HOVER DISTRICT NAME (Danial: selecting a district killed the finder — should stay; on hover, district name below "Sarawak" in smaller font). (1) seatCardHTML now emits the `.state-district-find-top` finder at the TOP when `state.openState && !MOBILE_MAP_INSPECT_MQ.matches` (dropdown shows the current seat.code, so switching is one tap; mobile keeps its sticky bottom switcher). (2) `#state-label` restructured from a bare textContent into `.label-main` (name+tier chip) + `.label-sub` (hovered district, smaller/dimmed) via replaceChildren; new `setStageSubLabel(name)` sets the sub; isolated-view mousemove resolves the hovered seat → sub, clearHoverUI + mouse-off clear it; sub height reserved only `body.state-open` (min-height:1em) so "Sarawak" never jumps. verification headless 1280px: hover P.195 → sub "Bandar Kuching" under "Sarawak" (shown), clears on mouse-off; select district via dropdown → seat-detail keeps finder at card-top (P.197 Kota Samarahan); screenshots eyeballed; mobile-sync + PRN + DUN-context regressions PASS, 90 tests, validate 4/4. Staging ee17c04c. · next: Danial hard-refresh + eyeball; promote-all pending; news 2/172.

2026-07-04 22:26 +08 · Codex · files: GAP_ANALYSIS_2026-07-04.md · commit: none · FULL UI/GAP AUDIT written. Verification: 90 lib tests PASS, app/lib syntax PASS, validate.sh PASS; headless Chrome across desktop 1440, short desktop 1201, mobile 390, tablet 768, reduced-motion mobile. Key bugs found: mobile search results visually exist but tap hits `svg#map`; `#q` focus-visible ring overridden by later `outline:none`; seat-detail share/card actions hidden because parent `.state-head` is hidden; keyboard Enter in district dropdown leaves list open and blocks tabs on short desktop; local dev console 404 noise for absent scores + `/api/live/johor`; PRN badge target is 168x28 (<44px high); mobile PRN mode lacks campaign summary context. Raw artifacts: `/private/tmp/mypolitik-audit/targeted-audit.json` + screenshots. · next: fix in this order: mobile search stacking, q focus ring, share actions in seat detail, dropdown keyboard close/select, dev API fallback, PRN mobile summary.

2026-07-05 00:54 +08 · Codex · files: public/app.js, public/styles.css, dev-server.py · commit: none · AUDIT FIXES APPLIED. Fixed active-search stacking globally so result taps hit the list instead of `svg#map`; re-asserted `#q` focus-visible after the late focus override; added visible Share link + Download card buttons inside normal and PRN seat details; made district picker keyboard selection immediate and closed on Enter/Space; disabled the intentionally absent gated scores fetch while preserving Skor gating; made local live PRN fetch prefer `data/live-johor.json` and added `/api/live/johor` dev-server fallback; raised PRN badge hit target to 44px; added compact mobile PRN campaign summary in the inspect tray. Verification: node --check app/lib/i18n PASS, python py_compile dev-server + pipeline PASS, 90 lib tests PASS, scripts/validate.sh PASS. Headless Chrome PASS: mobile search result hit BUTTON + selected Bangi; short desktop search hit BUTTON + ArrowDown/Enter district picker closed (`aria-expanded=false`) with 44px visible share/card actions; live badge measured 172x44; mobile PRN summary measured 340x80; console logs clean. · next: Danial visual check on phone/desktop, then commit/promote if desired.

2026-07-05 01:09 +08 · Codex · files: public/app.js · commit: none · STATE ISOLATION MOTION FIX. Open-state choreography now suppresses early map refits, preserves the clicked state's screen position through the panel/map-band layout swap via `setViewBoxPreservingScreen`, then runs one 720ms viewBox camera move into the final state frame. This makes the state travel into place instead of appearing above its original location. Verification: node --check public/app.js PASS, 90 lib tests PASS, scripts/validate.sh PASS, git diff --check PASS. Headless Chrome: Sarawak state bbox top moved 307px → 211px → 108px through the transition with clean logs; reduced-motion reaches state-summary; mobile search still selects Bangi and lands on `#parlimen/parti/P.102`. · next: phone/desktop visual check for animation feel.

2026-07-05 01:18 +08 · Codex · files: public/app.js · commit: none · STATE ISOLATION NO-DELAY TUNE. Removed the remaining 300ms wait for the old overview-card minimize choreography, so `revealStateCard({ animateState:true })` starts in the click path and the state camera move begins immediately while the card animation runs alongside it. Verification: node --check public/app.js PASS, 90 lib tests PASS, scripts/validate.sh PASS, git diff --check PASS. Headless Chrome early-frame sample: at 20ms panel already `state-summary`, viewBox already moved from full-map to `28.62 12.97 755.95 213.11`; Sarawak top progressed 265 → 228 → 186 → 139 → 107px with clean logs. · next: visual check on actual device for perceived immediacy.

2026-07-05 01:23 +08 · Codex · files: public/app.js · commit: none · SMOOTH RETURN FROM ISOLATED STATE. `backToControls()` now uses the same screen-position-preserving viewBox technique in reverse: keep the open state isolated, swap the panel/map band back to overview, preserve the state on screen, animate to the full Malaysia view over 560ms, then clear `instate/outstate` and remove `.isolated` after the camera settles so the rest of the map fades back in. Verification: node --check public/app.js PASS, 90 lib tests PASS, scripts/validate.sh PASS, git diff --check PASS. Headless Chrome outside-click sample: open Sarawak top 107px; after close top 107 → 130 → 263 → 306px, final viewBox full `0.00 0.00 799.85 352.74`, `.isolated=false`, logs clean. Reduced-motion open+close returns to `#panel.empty` with full viewBox and no console noise. · next: Danial visual check for reverse animation feel.

2026-07-05 01:30 +08 · Codex · files: public/app.js, public/styles.css · commit: none · RETURN FADE SYNC. Added a short-lived `#seats.returning` phase during click-outside state close so out-of-state paths fade from opacity 0 back to the full map over the same 560ms ease-in-out camera return, then clear with `.isolated` after settle. `openStateCard()` now also clears stale `returning` if a new state is opened mid-exit. Verification: node --check public/app.js PASS, 90 lib tests PASS, scripts/validate.sh PASS, git diff --check PASS. Headless Chrome outside-click sample: outstate opacity 0 before close, then 0.002 @20ms, 0.151 @120ms, 0.785 @280ms, 0.984 @460ms, 1.0 @660ms while viewBox returns to full; logs clean. Reduced-motion smoke returns to `#panel.empty`, full viewBox, opacity 1, no stale classes. · next: Danial visual check on device.

2026-07-05 01:39 +08 · Codex · files: public/app.js · commit: none · DISTRICT DETAIL NO-EXPAND FIX. Added `preserveMapView` support to `animateCardResize()` so district-detail card swaps can resize the card while rewriting the viewBox to keep the isolated state pixel-stable on screen; skipped SVG FLIP for that path, suppressed map-inspect refits, removed the mobile delayed detail glide, and applied the same preservation to detail tab/back swaps. Verification: node --check public/app.js PASS, 90 lib tests PASS, scripts/validate.sh PASS, git diff --check PASS. Headless Chrome: desktop Sarawak bbox stayed 440.74x297.34 from isolated through 20/180/520ms after district click with `transform:none`; mobile map-inspect More stayed 344.19x232.20 through detail swap while map band changed 674→197; reduced-motion mobile stayed 344.19x232.20 → 344.18x232.20, logs clean. · next: Danial visual check on device.

2026-07-05 01:45 +08 · Codex · files: public/app.js · commit: none · STATE TITLE OVERLAP FIX. `stateViewBox()` now measures the persistent state label and biases the state frame downward only when the label's bottom edge would collide with northern geometry, keeping the title above the SVG instead of overlaying it. Verification: node --check public/app.js PASS, 90 lib tests PASS, scripts/validate.sh PASS, git diff --check PASS. Headless Chrome at 1571x1568: Sabah title bottom 164px, Sabah SVG top 177.47px, gap +13.47px in both state summary and Ranau detail; all-state desktop sweep found 0 overlaps, worst gap still positive at +11.77px (Melaka), console logs clean. · next: Danial visual check on browser.

2026-07-05 01:51 +08 · Codex · files: (deploy only) · commit: 146f4cc · PROD DEPLOYED. Danial asked "push to main"; this repo has no git remote and project convention is Wrangler prod deploy. Fast-forwarded local `master` to staging commit `146f4cc` (`MyPolitik: fix map interaction polish`), then ran `npx wrangler deploy` with Cloudflare deploy env. Production Worker `mypolitik` deployed version `ccda11fc-f862-464a-860e-ab3f1fcfd4c9`; routes: mypolitik.xyz, www.mypolitik.xyz, mypolitik.krackeddevs.com. Live checks: `https://mypolitik.xyz` 200, `https://mypolitik.krackeddevs.com` 200, `https://www.mypolitik.xyz` 301 -> apex. `npm run deploy` failed first because `wrangler` was not on PATH; `npx wrangler deploy` succeeded. · next: Danial hard-refresh prod and eyeball the map interaction polish.

2026-07-05 · Claude (laptop) · files: pipeline/09_politicians.py, pipeline/wikidata_seat_map.json, public/data/politicians.json, public/assets/politicians/*.webp (93), public/app.js, public/index.html, public/styles.css, public/i18n.js, scripts/validate.sh · commit: 7bbcee8 · 🧑‍⚖️ POLITICIAN ROSTER (plan memoized-moseying-orbit.md), staging v b0e4ee23. Research (3 Explore agents): Wikidata→Commons is the ONLY machine-readable + licence-clean source; official Parliament portraits are govt-copyright (50yr) + JS/bot-walled = off-limits; ~42% MP photo coverage, ADUNs poor (phase 2). DATA: 09_politicians.py (resumable — SPARQL/img/bio cached under pipeline/raw/politicians/) pulls 222 MPs via P39=Q21290861 + pq:P2937=Q115641712 (term qualifier → exactly 222; the "no end date" heuristic gives 654 stale — DON'T use). Crosswalk: district QID (pq:P768, 100%) → our P.xxx by name-match (alias tanjong→tanjung) + verified vs results-ge15 winners; 45 name "diffs" are all same-person common-name forms (Hannah Yeoh/Yeoh Tseow Suan, Bung Moktar, Jeffrey Kitingan…), NOT by-elections — so display Wikidata common name + ballot_name subtitle; 1 label unresolved (Rafizi → QID) falls back to ballot. Photos: Commons imageinfo (iiurlwidth=320) → Pillow webp in public/assets/politicians/ (93 files, 1.1MB, committed) + per-image credit from extmetadata (68% attribution-required). Bios: Wikipedia REST summary en+ms verbatim + link. Identity/party/coalition from trusted results-ge15 (matches partyColor). ⚠️ Wikimedia needs descriptive User-Agent (403 else); WDQS 60s timeout (constrain queries). UI: enriched Current-YB block (photo + common name + ballot subtitle + age + education + clamped 4-line Wikipedia bio + socials + credit; initials-monogram fallback w/ hashed colour, zero licence risk); NEW #politicians directory (top-bar people icon → openPoliticians; searchable/filterable 222-card grid; card→openSeatFromPolitician→select(code); bootHash capture so #politicians deep-links survive writeHash; popstate closes). i18n EN+MS (pol_* keys), theme-aware CSS, validate.sh 5th check (222 MPs + photo files + credits). VERIFIED headless desktop+mobile: Anwar card photo/bio/socials/credit, monogram fallback (RR), directory 222/93 + search "anwar"→1 + GPS filter→23 + card-click→seat, mobile 2-col compact. 90 lib tests, validate 5/5, PRN+ctx PASS. ⚠️ mp-sync-drive (mobile pop timing) flakes ENVIRONMENTALLY — fails identically on stashed clean tree (same bytes that passed earlier this session) = machine-load/rAF-throttle after many headless runs, NOT this change; Danial confirmed the pop feels right on-device. NEXT: Danial eyeball staging; phase 2 = ADUN roster (same pipeline, sparse photos) + Kracked OSINT; promote-all to prod still pending.

2026-07-05 · Claude (laptop) · files: public/app.js, public/index.html, public/styles.css, public/i18n.js · commit: c736f24 · POLITICIAN PROFILE POP-UP, staging v 5ebeafaf (Danial: directory card should open a full-data pop-up, NOT navigate to the map). New native `<dialog id="pol-modal">` + `openPoliticianModal(code)`: large photo/monogram, common name + ballot subtitle, party pill, seat·state, stat tiles (age/DOB + GE15 majority + turnout from results-ge15 via formatResultCard), FULL unclamped Wikipedia bio, education, socials, Wikipedia+Wikidata links, photo credit, and an explicit "View seat on map →" button (openSeatFromPolitician). Directory card click rewired openSeatFromPolitician→openPoliticianModal; close via X/backdrop/native-Esc returns to directory. i18n EN+MS. Verified headless desktop+mobile 390px: card→modal opens over dimmed directory WITHOUT touching the map, full data renders, X returns to directory, View-seat opens seat detail. Updated scratchpad mp-politicians.js last assertion (card→modal now, not seat). 90 lib tests, validate 5/5. · next: Danial eyeball; promote-all to prod pending; orphaned politics-staging worker cleanup pending Danial's OK.

2026-07-05 · Claude (laptop) · files: pipeline/09_politicians.py, public/data/politicians.json, public/app.js, public/i18n.js · commit: 8788f84 · EXPANDED VERIFIED SOCIALS (Danial: "find all their socials + add links, verified"). VERIFICATION = the source: Wikidata best-rank `wdt:` claims EXCLUDE deprecated-rank (editor-flagged-wrong) accounts — no web-scraping/guessing handles (which is how parody/namesake accounts get in; violates the 04-doctrine). Pipeline now pulls P2013 FB(164) / P2003 IG(105) / P2002 X(98) / P7085 TikTok(18) / P856 website(13) / P2397 YouTube(7) / P3789 Telegram(3); 175/222 have ≥1. socialLinksHTML renders each w/ correct URL shape (tiktok.com/@, youtube.com/channel/, t.me/, website as-is). Coverage barely rose (Wikidata already had ~all it holds — FB dominates); the 47 w/ none stay blank (won't fabricate). Cleared sparql cache to re-query; photos/bios stayed cached (fast rebake). Verified headless: Awang Hashim FB/IG/X/TikTok correct hrefs; modal+dir regressions PASS; 90 tests, validate 5/5. Staging 7e151610. · next: Danial eyeball; prod promote + orphaned politics-staging cleanup still pending his OK.

2026-07-05 · Claude (laptop) · files: pipeline/politicians_socials_manual.json, pipeline/09_politicians.py, public/data/politicians.json, public/{app.js,i18n.js,styles.css} · commit: 5a2bf3c · FILLED 47 MISSING MP SOCIALS, staging v 15709dc2 (Danial: manually search + update). 3 general-purpose agents web-searched the 47 MPs w/ no Wikidata socials, CONSERVATIVELY (confirmed vs name+exact seat/state/party + source links; explicitly excluded namesakes/parody — e.g. zahirhassan.com=different person, @ayuhsepanggar=supporter acct). Found 44/47; 3 genuinely blank (P.120 Fong Kui Lun, P.220 Anyi Ngau, P.221 Hasbi Habibollah). Useful source discovered: mymp.org.my civic aggregator (mymp.org.my/p/<slug>). Stored in `pipeline/politicians_socials_manual.json` (44 MPs; FB44/IG31/X24/TikTok16/web7), merged by 09_politicians.py ONLY where Wikidata empty, flagged `socials_source:"community"` — survives Wikidata re-bakes. UI shows community socials DISTINCTLY (dashed underline + "Community-sourced · not yet Wikidata-verified" note) so never passed off as verified. Coverage now 219/222 (175 Wikidata + 44 community). 9 MEDIUM-confidence (double-check): P.002,030,033,074 (FB-only via aggregator), P.136 IG, P.154 all, P.188,206,214 (numeric-ID FB pages). Numeric FB slugs (facebook.com/<number>) for P.002,033,188,206,214. PARTY-LABEL CORRECTIONS surfaced (not applied — our labels=GE15 result): P.030 now Independent (ex-Bersatu), P.154 Bersatu (not PAS), P.219 PKR (not DAP), P.136 now PAS. Verified headless: Wong Chen modal FB/IG/X/TikTok/web + community note + dashed. validate 5/5, 90 tests. · next: Danial spot-check on staging; promote-all + orphaned politics-staging cleanup still pending.

2026-07-05 · Claude (laptop) · files: public/app.js, public/styles.css · commit: 75ee610 · SOCIAL ICONS everywhere, staging v 04c0672c (Danial: social logos on seat cards + mini directory cards + pop-up, as icon links). socialLinksHTML now renders inline brand-glyph SVGs (SOCIAL_ICONS map: fb/ig/tw/tiktok/youtube/telegram/web-globe, currentColor theme-safe, aria-label per platform) instead of text; SOCIAL_META = [label, urlFn] per platform, SOCIAL_ORDER. Added compact icon row to directory mini cards (politicianList now carries socials+socials_source). ⚠️ mini card `<button>`→`<div role=button tabindex=0>` (button can't nest `<a>`): POL_VIEW click skips `e.target.closest('a')` so an icon-click opens the LINK not the modal; card body opens modal; keydown Enter/Space opens modal. Community socials keep dashed outline + note (non-compact). Verified headless: 219/222 cards show icon links (svgs render, `<a href>`); modal(Wong Chen) 5 icons + community note + dashed; seat card(Anwar) 5 icons; MODAL+PRN regressions PASS; 90 tests, validate 5/5. · next: Danial eyeball; promote-all + orphaned politics-staging cleanup still pending his OK.

2026-07-05 · Claude (laptop) · files: public/app.js, public/styles.css · commit: 38f7d4a · MINI-CARD REDESIGN, staging v 616f059b (Danial: less whitespace — socials under the "P.xxx·seat" line, party badge on photo top-right). Party pill → `.pol-card-badge` absolute top-right of `.pol-card-photo` wrapper (drop shadow for legibility on photos); removed `.pol-card-foot` row so socials sit directly under seat; card padding 12→10, gap 8→6, align-content:start. Card height ~284px. Verified headless + screenshot (badges on photos, no foot, tight); modal regression PASS; 90 tests, validate 5/5. · next: Danial eyeball; promote-all + orphaned politics-staging cleanup pending his OK.

2026-07-05 · Claude (laptop) · files: public/app.js, public/styles.css · commit: 483d3f9 · UNIFORM MINI CARDS + 1-LINE SOCIALS, staging v 634fd055 (Danial: all cards same height; socials one line, cap 4, rest when opened). socialLinksHTML gains opts.max → compact cards render ≤4 icons on one flex-nowrap line + a "+N" chip; modal (no max) shows ALL. Uniform height via reserved sections: name -webkit-line-clamp:2 + min-height 2.4em, seat 1-line nowrap+ellipsis, socials always a 26px row (`.pol-card-socials-spacer` for the 3 MPs w/ none); photo 1:1 at equal 1fr column width → identical card height. Verified headless: all 222 cards EXACTLY 271px (1 distinct height), ≤4 icons/26px row, +N chip present, modal full set; modal regression PASS, 90 tests, validate 5/5. · next: Danial eyeball; promote-all + orphaned politics-staging cleanup pending his OK.

2026-07-05 · Claude (laptop) · files: public/data/johor-pledges.json, public/app.js, public/i18n.js, public/styles.css, pipeline/06_candidate_news.py · commit: 7a7c755 · PRN JOHOR CANDIDATE ENRICHMENT — manifesto pledges + news-bake rework, staging v 461f8024 (Danial: enrich candidates + news focused on Johor). PLEDGES (agent-researched, sourced, SHIPPED): johor-pledges.json PH "Johor Untuk Semua" 10-pt + BN "TEKAD BN Johor" 63-pledge (8 top pledges each, faithful summaries + manifesto source links); PN omitted (no manifesto released as of 5 Jul — not fabricated). prnPledgesHTML(entry) shows the pledges of coalitions contesting THAT seat, coalition-attributed + "Read the manifesto" link; loadOptional johorPledges; EN+MS. Verified N.02 Jementah: PH+BN blocks, 16 pledges, 2 links. NEWS: reworked 06 to burst-friendly (429→skip fast, leave unrecorded for a later pass; was 3× long backoff that stalled; THROTTLE 3s). ⚠️ GDELT now HARD-blocks the laptop IP (every req 429s both attempts — earlier intermittent 200 was luck); 172-candidate bake CANNOT complete from here. News still 2/172. OPTIONS for Danial: (a) run GDELT bake from VPS clean IP (needs his OK), (b) switch news source to Google News RSS (reliable but ToS=personal-use — his call for a public app), (c) wait for GDELT cooldown. · next: Danial decides news route; promote-all + orphaned politics-staging cleanup pending.

2026-07-05 · Claude (laptop) · files: public/app.js, public/i18n.js · commit: eb4ef54 · FIX: Johor DUN seats showed same YB (user feedback: "YB semerah n parit sulong sama / parit raja n parit yaani sama"). ROOT CAUSE confirmed: Johor has NO state result in results-dun.json (only 2023 six-state PRN), so Johor DUN → parent-Parliament GE15 MP fallback; all 26 Johor Parliament seats have 2-3 DUN children → siblings show the SAME MP (Parit Yaani+Parit Raja both P.149→Aminolhuda; Semerah+Sri Medan both P.147→Noraini). FIX: `johorDunResult(seat)` builds a per-seat result from `incumbent_2022`/`incumbent_party_2022` ("BN-UMNO"→coalition BN, party UMNO)/`majority_2022` already in prn16-johor.json; `ownDunResult()` returns it before the parent fallback → flows to YB card + choropleth colour + tooltip + state summary. New `src_johor2022` label (EN+MS) replaces mislabelled PRN-2023 + removes the parent-level note. Verified headless: Parit Yaani→Mohamad Najib Samuri, Parit Raja→Nor Rashidah Ramli (DISTINCT), Semerah→Mohd Fared Mohd Khalid; PRN+ctx regressions PASS, 90 tests, validate 5/5. Staging cc96c473. NOTE: general lesson — any state whose PRN isn't in results-dun.json has this fallback; N9 will need the same treatment. · next: promote-all + politics-staging cleanup + em-dash copy sweep + news route all pending Danial.

2026-07-05 · Claude (laptop) · files: public/app.js, public/i18n.js · commit: 14dac32 · SCOPE-WIDEN of the Johor YB fix. Audited ALL states (scratchpad audit over seats-dun.json vs results-dun.json): results-dun.json only holds the 2023 SIX-STATE PRN (codes 2/3/5/7/10/11 = Kedah/Kelantan/N.Sembilan/Penang/Selangor/Terengganu). Every OTHER DUN state fell back to the parent-Parliament GE15 MP → siblings show SAME federal MP as "Current YB". Still-buggy after Johor: Melaka(28)/Pahang(42)/Perak(59)/Perlis(15)/Sabah(73)/Sarawak(82) = 299 DUN seats. candidates-dun-prn15.json ALSO covers only those 6 PRN states, so no in-repo winners for the others. FIX: resultFor() drops the parent-MP fallback for DUN tier — DUN shows own result (covered PRN or Johor prn16 incumbent) or an honest empty state; reworded dun_note (EN+MS, no false "results shown at parent level"); parti choropleth stops colouring these by federal parent. Verified headless: Johor distinct (Najib Samuri/Nor Rashidah), Perak siblings both show note (no repeated MP), Selangor unchanged (RIZAM/SALLEHEN). PRN+ctx PASS, 90 tests, validate 5/5. Staging fe6024e4. FOLLOW-UP (data-sourcing, not code): to actually SHOW state assemblymen for the 6 states, source per-seat winners — Sabah PRN2020, Melaka+Sarawak PRN2021, Perak+Pahang+Perlis PRN w/ GE15 Nov2022 — into results-dun.json (or per-state incumbent files like prn16). · still pending Danial: promote-all to prod, politics-staging cleanup, em-dash sweep, news route.

2026-07-05 · Claude (laptop) · files: pipeline/03_results_dun.py, public/data/results-dun.json, public/lib.js, public/app.js, public/i18n.js · commit: 0c49907 · BEST-DATA follow-through on the DUN audit ("make sure no turn is left unturned"). Sourced REAL state assemblymen for the 6 remaining DUN states from Thevesh's Malaysian Election Corpus (github.com/Thevesh/paper-meco-results, data/consol_ballots.csv — single coalition-resolved ballot file, ALL Malaysian elections; also lookup_party.csv for party_full). 03_results_dun.py extended: after the 2023 six-state PRN, build_meco_states() adds each state's MOST RECENT state general election → Sabah 2025 (SE-15, NOT stale 2020!), Sarawak 2021 (SE-12), Melaka 2021 (SE-15), Perak/Pahang/Perlis 2022 (SE-15). +299 seats → results-dun.json now 544/600 (only Johor's 56 out, by design via prn16). Crosswalk 100% (all 299 match seats-dun.json N.xx codes; verified MECO-only=0, ours-missing=0). BY-ELECTION overlays (current holder, not general winner): 12_N.58 Sabah 2026, 8_N.48 Perak 2025, 6_N.36 Pahang 2023, 13_N.67 Sarawak 2023 (flag _byelection). MECO coalition 'ALONE' (solo runners) relabeled to party → WARISAN25/BEBAS5/PSB4/UPKO3/STAR2/KDM1; added STAR/UPKO/PSB to COALITION_COLORS(lib.js)+COALITION_ORDER(app.js) so choropleth+pills+summary bar complete. New i18n src_state_election/src_byelection (EN+MS) name the exact election; seatCardHTML resultSource picks johor2022 > election(byelec/state) > prn15 > ge15. MECO CSVs cached in pipeline/raw/ (meco_consol_ballots.csv ~4.4MB, meco_lookup_party.csv). Verified headless (8 cases): Perak siblings distinct (Mohamad Amin/PN vs Salbiah/BN), by-elec seats show current holder+label, Sabah WARISAN + Sarawak GPS colours right, Selangor+Johor unchanged. PRN+ctx PASS, 90 tests, validate 5/5. Staging 956a013e. NOTE: dun_note copy now effectively dead (all 600 DUN seats have data) — kept as fallback. NOTE re-running 03 refreshes everything incl. MECO (self-contained). · still pending Danial: promote-all to prod, politics-staging cleanup, em-dash sweep, candidate-news route.

2026-07-05 · Claude (laptop) · PROD PROMOTE (Danial approved). `npx wrangler deploy` (prod worker `mypolitik`) → LIVE at mypolitik.xyz / www.mypolitik.xyz / mypolitik.krackeddevs.com, Version 4fd0dfea. Ships the whole session's staging work: Johor DUN YB fix + all-states parent-MP fix + MECO real assemblymen for all 6 remaining DUN states (results-dun.json 544/600, +4 by-election overlays) + STAR/UPKO/PSB colours + src_state_election/src_byelection i18n. Assets dir = ./public only, so pipeline/wikidata_seat_map.json (M, pipeline-only, noisy reorder) + moved-out scratchpad-sweep.js do NOT ship; public/data/candidate-news-johor.json (M, 3-cand news) DID ship (already on staging, parity). Prod smoke: homepage 200, results-dun.json 544 entries, headless render PASS (Perak 8_N.01 Mohamad Amin/"Perak 2022", Sabah by-elec 12_N.58 Mohd Ismail/"by-election 2026", Sarawak 13_N.01 Billy/"Sarawak 2021"). NOTE working tree still has 2 uncommitted M files (wikidata_seat_map.json pipeline reorder, candidate-news-johor.json) — deployed but not committed. · still pending Danial: politics-staging worker cleanup, em-dash sweep, candidate-news source route.

2026-07-05 · Claude (laptop) · files: public/index.html, public/app.js, public/styles.css, public/i18n.js, public/lib.test.mjs · commit: dd24078 · FEATURE: wide-screen BENTO dashboard for the Johor PRN (Danial: "bento style dashboard for bigger aspect ratios, esp johor pru"). On >=1180px, entering PRN mode opens a full spatial dashboard (#prn-bento, spans app grid under the fixed topbar via body.prn-bento-on, gated in CSS+JS matchMedia PRN_BENTO_MQ). Tiles: countdown hero (days→polling), key dates, self-rendered CLICKABLE Johor choropleth (built from DUN seat .d paths, coloured by johorDunResult/2022 winner or live; NO #map reparenting), coalition standings+majority, "Are you registered?" CTA, seat SPOTLIGHT (candidates+incumbent, updates on bento-map click via prnBentoSeat — self-contained, doesn't touch state.selected/panel), PH/BN pledges. Wired: openPrnMode shows it (preserves preSel seat since openStateCard clears state.selected), closePrnMode = single hide point (home/politicians/close all route thru), refreshPrnLive re-renders. New prn_bento_* i18n EN+MS. Fixed 2 stale data-lock tests from the earlier DUN work (COALITION_COLORS +STAR/UPKO/PSB; results-dun 544/600 six-state-as-subset) — NOTE these 2 were already failing when I prod-promoted 4fd0dfea (data-lock only, not functional). Verified headless (mp-bento-drive.js): 7 tiles/56 seats/countdown/preselect Buloh Kasap/click→Paloh at 1600, hidden+panel-shown at 900. Moved mp-prn-drive desktop 1280→1120 (panel PRN flow lives 860-1180; 1280 now = bento). 91 tests, PRN+ctx+bento PASS, validate 5/5. STAGING 14a1d51e — NOT prod (show Danial first). Polish TODO if he likes it: dashboard is taller than viewport at 1050h (pledges scroll) — could tighten to fit; selected-seat outline; hook search/finder to bento spotlight. · pending: prod-promote bento (after review), politics-staging cleanup, em-dash sweep, news route.

2026-07-06 · Claude (laptop) · commits 2a-c · MANIFESTOS + BENTO polish (Danial asks). (a) commit prior: BERSAMA + MUDA-PSM manifestos added to johor-pledges.json (researched via 2 web agents, sourced+attributed), PN = honest "pending/not released as of 5 Jul" state; shared pledgeBlockHTML() renders manifesto/note/pending across seat card + bento; prn_pledge_pending i18n; titles em-dash→"~". (b) commit 21360df, files pipeline/05_prn16_johor.py + public/data/prn16-johor.json + app.js + i18n.js + styles.css: TWO requests — (1) click district shows CURRENT HOLDER + votes: enrich_incumbent_votes() joins MECO consol_ballots Johor to add incumbent_votes_2022 + incumbent_pct_2022 = winner of most recent COMPLETED poll (SE-15 2022 OR later by-election; N.13 correctly picks 2023 by-elec Nazri over late Salahuddin Ayub); 56/56 enriched (name-check warns = benign bin/Abd formatting); incumbentBlockHTML "Currently held by {name}·{party} / {v} votes ({p}%) · majority {m}" placed under seat name in spotlight + as meta row in prnSeatCardHTML. (2) BENTO now 3 ROWS (meta strip · map+spotlight · pledges) vs tall 4-row; grid-template-rows auto minmax(340px,1fr) auto; **#prn-bento min-height:0** = the fix so the 1fr map row shrinks to viewport (grid-item min-height:auto was overflowing to 1246px); map-wrap fills cell (removed place-items:center shrink). Verified headless 1600+2000px, incumbent line correct (Paloh→Lee Ting Han 8,077/55.1%/maj3,176). 91 tests + PRN+bento+ctx regressions + validate PASS. STAGING 40375a77 — NOT prod. NOTE re-check PN manifesto 8-10 Jul (may drop before polling). · pending: prod-promote (bento+manifestos+votes after review), politics-staging cleanup, em-dash sweep (partly done in johor-pledges titles), candidate-news route.

2026-07-06 · Claude (laptop) · files: public/app.js, public/styles.css · commit dac8d7a · Bento spotlight: candidate ROWS → candidate CARDS (Danial: "add candidate cards, fits right of the map"). prnCandidateCardsHTML(entry, seatCode): coalition-accented left border (--cc), name + ballot alias, party pill, ballot symbol (🗳, only 6/172 have one), + campaign headlines from prnNews for the 3 seats that have them (N.01/N.02/N.06) rendered as links INSIDE the card. `.bento-cand-grid` auto-fit repeat(minmax(210px,1fr)) → 2-3 cols by width; 4-way seat packs under the incumbent block, fits row2. Verified headless 1680: Paloh 4 cards (incl BEBAS "Kereta Api" symbol), Buloh Kasap 2 cards w/ Zahari Sarip's 2 Star headlines. bento-drive+prn-drive+ctx+validate+91 tests PASS. STAGING 54d11764 — NOT prod. · pending: prod-promote (bento+manifestos+incumbent-votes+cards after review), politics-staging cleanup, em-dash sweep, news route (only 3/172 have news — GDELT laptop-IP blocked).

2026-07-06 · Claude (laptop) · files: public/app.js, public/styles.css · commit cbed3e5 · Bento manifestos: 5 cramped columns → TABS (Danial: "not clear, use tabs, whichever they click has the info"; also fixed the source-line overlapping the pledges). prnPledgeTabsHTML()+prnPledgeOrder(): tab bar (PH/BN/PN/BERSAMA/MUDA-PSM, coalition-colour dot, active pill) + single .prn-pl-panel showing the selected bloc's pledgeBlockHTML; PRN_BENTO click handler branch on .prn-pl-tab → set prnPledgeTab, re-render #bento-pledge-body. Panel pledge list = multi-column grid (repeat auto-fill minmax(250px,1fr)) across the full-width row 3 → compact + consistent height; whole dashboard now fits ONE viewport (gridH 886, no scroll) so the .bento-foot source line no longer overlaps. Verified headless 1680: PH(8)→BERSAMA(5)→PN(pending) switching works. bento-drive+prn-drive+ctx+validate+91 tests PASS. STAGING 8b886ab0 — NOT prod. · pending: prod-promote (whole Johor-bento run: real assemblymen all states + manifestos + incumbent-votes + candidate-cards + tabs), politics-staging cleanup, em-dash sweep, news route.

2026-07-06 · Claude (laptop) · files: public/app.js, public/styles.css · commit d413da8 · Bento now on LANDSCAPE TABLETS (Danial: "bento even for iPad-size aspect ratios"). Breakpoint 1180→1000px (CSS @media + PRN_BENTO_MQ matchMedia, kept in sync) → iPad landscape 1024/1180/1366 get bento; phones + portrait tablets (≤1000) keep map+panel flow. Fixed spill bug it surfaced: .bento-grid had min-height:0 → on short (768/834 tall) viewports it shrank below content and pledges overflowed onto the .bento-foot source line. Removed min-height:0 → grid grows to fill tall (flex:1) but never below content, so short screens scroll #prn-bento cleanly (foot always beneath grid). Added @media (min-width:1000px) and (max-height:840px) tighten pass (row2 min 340→270, smaller padding/countdown) for short iPads. Verified headless: renders at 1024×768 / 1194×834 / 1366×1024 (7 tiles, 4 cards, 5 tabs, NO foot overlap), iPad portrait 820×1180 stays panel. mp-prn-drive desktop moved 1120→940 (panel flow now lives 860-1000). 91 tests + bento+prn+ctx+validate PASS. STAGING ea4e68ad — NOT prod. · pending: prod-promote whole Johor-bento run, politics-staging cleanup, em-dash sweep, news route, re-check PN manifesto 8-10 Jul.

2026-07-07 · Claude (laptop) · commit (v=4 bump) + verify · "hasn't changed on staging" DIAGNOSIS: deploy was FINE — verified headless on LIVE staging.mypolitik.krackeddevs.com at 1024×768 with Network.setCacheDisabled: bento renders (7 tiles, 5 tabs, 4 cards, "Paloh" spotlight), matchMedia(min-width:1000px)=true, live-badge click opens it. Root cause = STALE BROWSER CACHE: every deploy this whole session reused `?v=3` on app.js/styles.css/lib.js/i18n.js, so a browser that cached an early ?v=3 copy kept serving it (assets have cache-control max-age=0 must-revalidate + etag, but identical ?v=3 URL means some caches/Safari never refetched). FIX: bumped ALL ?v=3→?v=4 (index.html: styles.css+app.js; app.js imports: lib.js+i18n.js), redeployed staging 6e2d638d. Brief post-deploy edge blip served stale ?v=3 HTML once (cf-cache-status HIT) then settled to ?v=4 (confirmed 3× consecutive). No service worker. **GOTCHA for future: BUMP `?v=N` in public/index.html + the two imports in public/app.js whenever JS/CSS changes, else browsers serve stale.** Also: bento ONLY shows in PRN election view (tap "PRN JOHOR · 11 JUL" badge) on landscape ≥1000px — not on the plain map. · pending unchanged: prod-promote whole run, politics-staging cleanup, em-dash sweep, news route, PN manifesto re-check 8-10 Jul.
