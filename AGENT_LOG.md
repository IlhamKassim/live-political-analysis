# AGENT_LOG — Peta YB (VPS night shift)

Append-only handoff log. Newest at the bottom.

2026-06-27 16:03 UTC · Codex · files: scripts/validate.sh, NIGHT_QUEUE.md, AGENT_LOG.md · commit: BLOCKED (.git/index.lock cannot be created; .git is read-only in sandbox) · verification: `scripts/validate.sh` => PASS node --check public/app.js; PASS python3 -m py_compile pipeline/*.py; PASS JSON.parse public/data/*.json; PASS GE15 coalition tally · next: restore git write access and commit the validation-script task changes.
2026-06-27 16:04 UTC · Codex · files: NIGHT_QUEUE.md, AGENT_LOG.md · commit: BLOCKED (.git/index.lock cannot be created; .git is read-only in sandbox) · verification: no Claude commit present after base snapshot; reviewed `public/app.js`/`public/index.html` loadOptional path; `node --check public/app.js` PASS; `scripts/validate.sh` PASS all checks · next: loadOptional failure path confirmed intact: boundary `render()` completes before optional fetches, results/scores fetch failures are caught independently, and disabled mode buttons prevent unavailable hash modes from activating.

2026-06-27 16:20 UTC · Claude · files: public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 1a93061 · verification: CSS-only change (no JS/markup); `node --check public/app.js` PASS; styles.css braces balanced 94/94; new @media (max-width:480px) and (max-width:390px) blocks: search becomes a full-width row, tier/mode segs stretch to fill, results dropdown anchored left:0/right:0 (no horizontal overflow), brand tag hidden + tighter padding ≤390px. NOTE: scripts/validate.sh FAILs are environmental only (can't write logs to read-only /tmp in sandbox), not real failures. · next: i18n completeness pass (next unchecked (claude) task).

2026-06-27 16:40 UTC · Claude · files: public/index.html, public/app.js, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 0f7e90d · verification: `node --check public/app.js` PASS; EN/MS I18N key sets at parity and every data-i18n* key in index.html resolves (script check => [] missing both ways). Wired 5 previously-hardcoded user-facing strings through I18N: meta description (meta_desc), SVG aria-label (map_aria — was hardcoded BM "Peta kerusi Malaysia" even at EN default, now EN/BM), and the three control-group aria-labels Seat layer/Colour mode/Language (aria_tier/aria_mode/aria_lang). Added two new applyStatic handlers: data-i18n-aria→aria-label, data-i18n-content→content. · next: Accessibility pass (next unchecked (claude) task) — can build on data-i18n-aria for new aria-labels.

2026-06-27 17:05 UTC · Claude · files: public/index.html, public/app.js, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 6ce659e · verification: `node --check public/app.js` PASS; CSS braces balanced 98/98; EN/MS I18N at parity (45 keys each), all data-i18n* keys in index.html resolve. Accessibility pass: added aria-label to search input (#q, i18n search_aria) + #results listbox (results_aria); aria-label override on #reset (reset_aria, so the ↺ glyph isn't read literally); explicit aria-label on EN/BM lang buttons (English/Bahasa Melayu). Made role="tab" buttons announce state — added aria-selected to tier/mode/lang markup and a setOn(el,on) helper that keeps the "on" class + aria-selected in sync at all 5 toggle sites (setLang/setTier/setMode + 2 init). CSS: new :focus-visible ring (2px accent outline; inset -2px on .seg buttons since the segment clips overflow; re-asserted on .search input which zeroed its outline). Legend not colour-only: every swatch row already pairs with a text label; added a 1px ring to .legend/.sharebar-key swatches so low-contrast colours stay distinguishable. · next: Loading + empty states (next unchecked (claude) task).

2026-06-27 17:30 UTC · Claude · files: public/app.js, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: ef83ab9 · verification: `node --check public/app.js` PASS; CSS braces balanced 105/105; EN/MS I18N at parity (46 keys each), all data-i18n* keys in index.html resolve. Loading + empty states: (1) #loading text now has a subtle pulse affordance while boundaries load (CSS @keyframes loading-pulse, gated by prefers-reduced-motion). (2) Boundary fetch failure previously left the spinner hanging forever — render() is now wrapped in try/catch at init AND setTier; on failure showLoadError() swaps the overlay to a friendly i18n'd message (new load_error key EN+BM). setTier failure restores the previous tier + tab state (state.tier/buttons) so the old map stays usable; renderSummary() guards against missing data. All state swaps happen inside the existing position:absolute;inset:0;pointer-events:none overlay → zero layout shift. setLang re-applies the error copy after applyStatic so a language toggle during the error state stays translated. · next: queue has only (codex) hygiene task left for (claude) lane — no doable (claude) task remaining.

2026-06-27 18:00 UTC · Claude · files: public/index.html, public/app.js, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 0d8c197 · verification: `node --check public/app.js` PASS; `grep -rni "petayb\|peta yb" public/` => no user-facing leftovers; MyPolitik now in `<title>`, brand mark, and both EN/MS I18N `title` keys. Phase B brand rename → MyPolitik. Changed 4 user-facing strings + 1 source comment: index.html `<title>` "Peta YB —"→"MyPolitik —"; `.brand` mark `◧ PETA<b>YB</b>`→`◧ My<b>Politik</b>` (two-tone bold on "Politik"); app.js EN+BM I18N `title` keys (mirror the <title>, applyStatic sets document.title); app.js line-1 header comment. KEPT infra untouched: `localStorage` key `peta-yb-lang` (×2, would break saved lang pref) and the `brand_tag` taglines ("Your representatives, on the map" / "Wakil rakyat anda, atas peta" — descriptive, no brand-name leftover). #panel-empty held no brand string. [HVR] — structural/grep-verified only; brand-mark visual rendering is Danial's morning call. · next: codex VERIFY brand task, then Phase 0 keystone lib.js (next (claude) task).

2026-06-27 18:30 UTC · Claude · files: public/lib.js (new), public/lib.test.mjs (new), public/app.js, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 2b925a4 · verification: `node --test public/lib.test.mjs` => 11 pass / 0 fail; `node --check public/app.js` PASS; `node --check public/lib.js` PASS. Phase 0 keystone: created DOM-free ES module public/lib.js exporting pure `encodeHash(state)` (#tier/mode[/code], percent-encoded, falsy `selected` omitted) and `decodeHash(hash)` (strips leading #, null on empty, splits on "/", per-segment decodeURIComponent). Faithful extraction of app.js's old writeHash/parseHash internals — NO behaviour change: writeHash() now `encodeHash(state)`, parseHash() now `decodeHash(location.hash)`. app.js already had type="module" so the import needed no markup change. lib.test.mjs has 11 zero-dep cases incl. round-trip + the faithful quirk that absent trailing segments decode to `undefined` (not "") — old parseHash's `.map` only ran over present elements, so tests assert that, not a "fixed" empty-string. [MV] — machine-verified. · next: codex VERIFY+STRESS Phase 0 (break encode/decode with junk/empty/extra-field hashes), then Phase 1 geolocation core (next (claude) task).

2026-06-27 19:00 UTC · Claude · files: public/lib.js, public/lib.test.mjs, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 44472b6 · verification: `node --test public/lib.test.mjs` => 27 pass / 0 fail (11 prior + 16 new); `node --check public/app.js` PASS; `node --check public/lib.js` PASS. Phase 1 geolocation core. Added to lib.js: faithful `project(lng,lat)` (frozen krackedmaps constants verbatim from pipeline/01_boundaries.py INCL. the lng>=107 Borneo shift -240.0879); `parsePathRings(d)` (M/L/Z → rings of [x,y], tolerant of missing trailing Z + junk/null); `pointInRings(x,y,rings)` (even-odd ray-cast across all rings → holes handled by parity, matches SVG even-odd fill); bbox-prefiltered `findSeatForLocation(lat,lng,seats)` (project once → bbox skip → PIP, returns seat or null, NaN/non-array guarded); `haversine` + `nearestSeat(lat,lng,seats)` fallback over seat.ll. Tests load the REAL public/data/seats-parlimen.json and assert a 6-row fixture table resolves to the geographically-correct seat: KLCC→P.119 Titiwangsa, Putrajaya→P.125, Penang/George Town→P.049 Tanjong, JB→P.160, Kuching→P.195 Bandar Kuching, KK→P.172 Kota Kinabalu — the last two (lng 110/116) exercise the shift branch end-to-end. Plus unit tests: project viewBox bounds + a direct 107-boundary shift assertion (delta≈240.0879), parsePathRings happy/garbage, pointInRings square + hole, NaN/Paris→null, haversine KL→SG≈300km, offshore nearestSeat fallback, bad-input→null. No app.js changes (wiring is a later Phase-2 task). [MV] — machine-verified. · next: codex VERIFY+STRESS geolocation, then `pickInitialLang` pure fn (next (claude) task).

2026-06-27 19:30 UTC · Claude · files: public/lib.js, public/lib.test.mjs, public/app.js, NIGHT_QUEUE.md, AGENT_LOG.md · commit: c8eed18 · verification: `node --test public/lib.test.mjs` => 31 pass / 0 fail (27 prior + 4 new); `node --check public/app.js` PASS; `node --check public/lib.js` PASS. Phase 1 lang picker. Added pure `pickInitialLang(saved, navLanguages)` to lib.js: (1) explicit saved pref "ms"/"en" always wins; (2) else first recognised browser tag decides, case-insensitive, matching "ms"/"ms-*"/"ms_*" and "en"/"en-*"/"en_*", skipping unknown/non-string entries; (3) else BM-first default "ms". Wired boot: app.js now `lang = pickInitialLang(saved, navigator.languages)` inside the existing try/catch (catch falls back to pickInitialLang(null,null) → "ms"). This FLIPS the previous hardcoded default from "en" to BM-first "ms" when no saved pref + no en/ms browser tag — intended per queue ("ship the MECHANISM; final taste is Danial's"). 4 new tests: saved-wins, browser-decides (incl. case-insensitive + first-tag-wins + skip-unknown), BM-default fallback (empty/null/undefined/invalid-saved), non-string nav entries ignored. [MV] — machine-verified; the default-language taste is Danial's morning call. · next: codex VERIFY+STRESS lang picker, then Phase 1 card-format helpers (next (claude) task).

2026-06-27 20:00 UTC · Claude · files: public/lib.js, public/lib.test.mjs, NIGHT_QUEUE.md, AGENT_LOG.md · commit: cafa597 · verification: `node --test public/lib.test.mjs` => 44 pass / 0 fail (31 prior + 13 new); `node --check public/lib.js` PASS; `node --check public/app.js` PASS. Phase 1 card-format helpers. Added 4 pure DOM-free shapers to lib.js (no HTML — callers render, helpers only shape): `formatParty(r)` → {abbr, full, label} surfacing party_full ("Perikatan Nasional (PN)" when both differ, single value when same/one-missing, null when neither); `candidateCount(r)` → int>=1 surfacing n_candidates (truncates, rejects 0/non-number); `formatRunnerUp(r)` → {name, party, votes} surfacing runner_up.votes with per-field guards (null when no runner-up = single-candidate seat); `formatResultCard(r)` composes all + passes scalar numerics (votes/vote_pct/majority/majority_pct/turnout) through a Number.isFinite guard so legit 0s and 100% turnout survive where a falsy check would drop them. Two `str`/`num` module-local helpers. Tests load the REAL public/data/results-ge15.json: exact-shape assertion on P.001 Perlis fixture + a whole-dataset smoke loop (all 222 seats shape with name+party label+finite votes, no throw), plus synthetic edge cases (missing/partial/same party, single-candidate=no runner-up, 0 votes, 0% majority, 100% turnout, garbage/null input). No app.js wiring — renderPanel enrichment is the later Phase-2 task; these helpers are its building blocks. [MV] — machine-verified. · next: codex VERIFY+STRESS formatters (missing runner-up / 0 votes / 100% turnout / single-candidate), then Phase 2 "Find your YB" front door (next (claude) task).

2026-06-27 20:30 UTC · Claude · files: public/index.html, public/app.js, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 514ea02 · verification: `node --check public/app.js` PASS; CSS braces balanced 119/119; EN/MS I18N at parity (51 keys each); all 29 data-i18n* markup keys resolve in I18N; old empty_h/empty_p confirmed removed from both langs. Phase 2 "Find your YB" front door. Rewrote #panel-empty into a citizen-first action card: `.find-kicker` "FIND YOUR YB", `<h2>` "Who represents you?", `.find-sub` one-liner, a prominent full-width `#find-location` "📍 Use my location" button (var(--accent) fill, min-height:44px tap target), a "or search by name above" prompt + plain seat examples ("Titiwangsa"/"Bangi"/"Kota Kinabalu"). Demoted legend+summary into a collapsible `<details id="empty-more">` ("Explore the whole map", custom ▸/▾ marker) — KEPT the `#legend`/`#summary` element IDs so renderSummary() needs NO change and still populates them. Added 7 EN+BM I18N keys (find_kicker/find_h/find_sub/find_loc/find_or/find_eg/empty_more); removed the now-unused empty_h/empty_p from both langs. CSS: new `.find*` hero block + `.empty-more` disclosure styles in the panel section. NOTE: `#find-location` is intentionally UNWIRED this round — the geolocation→findSeatForLocation→select() wiring is the explicit next (claude) Phase-2 task; the button is inert until then. [HVR] — structural/grep/parity-verified only; visual layout + copy taste is Danial's morning call. · next: codex VERIFY front door (node --check; every new I18N key in BOTH en+ms — done, confirm; no untranslated strings), then Phase 2 wire geolocation button (next (claude) task).

2026-06-27 21:00 UTC · Claude · files: public/app.js, public/index.html, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 812979d · verification: `node --check public/app.js` PASS; `node --check public/lib.js` PASS; `node --test public/lib.test.mjs` => 44 pass / 0 fail (unchanged — no new pure logic, wiring only); CSS braces balanced 121/121; EN/MS I18N at parity (in-en-not-ms [] and in-ms-not-en [] both empty), all 5 new loc_ keys present in BOTH langs; all data-i18n* markup keys resolve. Phase 2 geolocation wiring. Imported findSeatForLocation + nearestSeat from lib.js. New `locate()` handler on #find-location: guards `navigator.geolocation` (→loc_unsupported), disables the button + shows loc_locating while pending, getCurrentPosition(highAccuracy, 10s timeout, 5min maxAge) → findSeatForLocation on the CURRENT tier's seats, PIP-miss falls back to nearestSeat, then existing select(seat.code) (reuses zoom + panel + hash). Error cb: code===1 → loc_denied, else loc_error. No-match → loc_notfound. Status surfaced in new `<p id="find-status" role="status" aria-live="polite">` inside .find — setFindStatus(key) stores key in dataset.i18n so setLang→applyStatic re-translates it; cleared on deselect() to drop stale messages. The search box + examples above remain the always-present manual fallback; every failure copy points back to it ("…search by name above instead"). CSS: .find-loc:disabled (cursor:progress;opacity:.7), .find-status pill. [HVR] — structural/grep/parity-verified only; the geolocation permission flow + visual states are Danial's morning call (no browser in sandbox). · next: codex VERIFY+STRESS geo wiring (denied/unsupported/timeout paths; fallback always reachable; node --check), then Phase 2 enrich renderPanel + Share/Copy-link button (next (claude) task).

2026-06-27 21:30 UTC · Claude · files: public/app.js, public/index.html, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: d6c1aff · verification: `node --check public/app.js` PASS; `node --check public/lib.js` PASS; `node --test public/lib.test.mjs` => 44 pass / 0 fail (no new pure logic — renderPanel wiring reuses the Phase-1 helpers); CSS braces balanced 129/129; EN/MS I18N at parity (61 keys each, in-en-not-ms [] / in-ms-not-en []); all 5 new keys (candidates/share_btn/share_ok/share_fail/src_ge15) present in BOTH langs; all data-i18n* markup keys resolve. Phase 2 renderPanel enrichment + Share/Copy-link. Imported formatParty/candidateCount/formatRunnerUp from lib.js and wired them into renderPanel: (1) party row now shows party_full label ("Perikatan Nasional (PN)" via formatParty, only when it adds info beyond the bloc pill — skips redundant "PN · PN"); (2) new "Candidates"/"Calon" row from candidateCount; (3) runner-up row now appends runner_up.votes (formatRunnerUp), with the pill guarded when party is null. Share: full-width `#share-link` button in a new `.seat-actions` block; delegated click on PANEL_SEAT (innerHTML rebuilds each render) → shareLink() builds `location.origin+pathname+encodeHash(state)` deep-link → guarded `navigator.clipboard.writeText` → transient `#toast` (share_ok / share_fail copy points to URL bar on failure). Transparency: per-card `.src-line` "Results: official GE15 (SPR · DOSM/Thevesh)" shown whenever results are present. CSS: .src-line, .seat-actions, .share-btn (44px tap target, accent hover), .toast (fixed bottom-center pill, .show transition, reduced-motion guarded). Share button label re-translates on setLang via the existing renderPanel re-render. [HVR] — structural/grep/parity/test-verified only; clipboard permission flow + visual layout/toast are Danial's morning call (no browser in sandbox). · next: codex VERIFY share/deep-link (hash round-trip via lib.test; open-on-load selects the seat; clipboard guarded), then Phase 2 onboarding clarity (next (claude) task).

2026-06-27 22:00 UTC · Claude · files: public/index.html, public/app.js, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 75f747b · verification: `node --check public/app.js` PASS; `node --check public/lib.js` PASS; `node --test public/lib.test.mjs` => 44 pass / 0 fail (no new pure logic — UI/wiring only); CSS braces balanced 145/145; EN/MS I18N parity (in-en-not-ms [] / in-ms-not-en []); 3 new keys (mode_soon, tap_hint, tap_hint_dismiss) each present in BOTH langs; all data-i18n* markup keys resolve. Phase 2 onboarding clarity. (1) Skor "Soon/Segera" pill: the gated Score tab no longer reads as a dead button — a small ::after badge (CSS `.seg button[data-after]::after`, accent-2 fill, guarded on [data-after] so an unset attr never paints an empty dot) shows "Soon"/"Segera". Driven by new `data-i18n-after="mode_soon"` on the button + a new applyStatic handler that sets `data-after` to the translated text (pseudo-element, so the button's textContent i18n can't clobber it). Tab STAYS disabled — gating unchanged. (2) First-load tap hint: new `#tap-hint` pill over the map (i18n tap_hint) nudges that seats are tappable; dismissed by its ✕, by select() (any seat), or remembered in localStorage key `peta-yb-hint-seen` (guarded try/catch; hintSeen/dismissHint/maybeShowHint). maybeShowHint() runs at end of init only when not seen AND nothing selected — a deep-linked seat skips the hint. (3) Reset discoverability: `#reset:not([hidden])` now slides+fades in (@keyframes reset-in) each time "Show all" appears so the way back catches the eye. All three animations gated by prefers-reduced-motion (tap-hint keeps a static translateX(-50%) centering fallback). [HVR] — structural/grep/parity/test-verified only; visual placement, copy taste, and the geolocation/localStorage flows are Danial's morning call (no browser in sandbox). · next: codex VERIFY onboarding (Skor stays gated even if scores.json present; localStorage guarded), then Phase 2 mobile-first sheet (next (claude) task).

2026-06-27 22:30 UTC · Claude · files: public/index.html, public/app.js, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: 7773916 · verification: `node --check public/app.js` PASS; `node --check public/lib.js` PASS; `node --test public/lib.test.mjs` => 44 pass / 0 fail (no new pure logic — UI/wiring only); CSS braces balanced 154/154; EN/MS I18N parity (65 keys each, in-en-not-ms [] / in-ms-not-en []); 1 new key (sheet_handle) present in BOTH langs; all 33 data-i18n* markup keys resolve. Phase 2 mobile-first sheet. On screens ≤820px the panel is now a draggable bottom sheet OVERLAYING the map (the map is the reward): grid drops the "panel" row so #stage takes the full height; #panel goes `position:fixed; bottom:0; height:85dvh` (85vh fallback) with a `transform:translateY(calc(100% - var(--sheet-peek)))` peek (--sheet-peek:172px, --sheet-pad:16px on #app so #reset can inherit the peek too). `.sheet-open` slides it to translateY(0); `.dragging` drops the transition for finger-follow. New full-bleed sticky `#sheet-handle` button (min-height:44px tap target, touch-action:none, 40×4 grip pill) toggles via tap/keyboard (click→setSheet, suppressed right after a drag) AND pointer-drag (pointerdown/move/up/cancel: live inline translateY clamped [0, height−peek], snaps open/closed at the halfway mark on release). select() calls setSheet(true) so a tapped/deep-linked/located seat opens the card; deselect() setSheet(false) collapses to peek. #reset floats above the peek (`bottom:calc(var(--sheet-peek)+12px)`). Removed the old `max-height:42vh/46vh` panel rules; ≤480 now just overrides `--sheet-pad:14px` + `#reset left:12px`. Desktop (>820px) is fully unaffected: `.sheet-handle{display:none}` base rule and no desktop `.sheet-open` rule, so setSheet calls are inert there. Safe-area-inset-bottom padding for notched phones; prefers-reduced-motion drops the slide transition. [HVR] — structural/grep/parity/test-verified only; the peek height, default-collapsed-on-load choice, drag feel, and visual layout are Danial's morning call (no browser in sandbox). · next: codex VERIFY mobile (42vh truncation fixed, tap-target sizes, no horizontal overflow at ≤390px; review pointer-drag + reduced-motion paths), then Phase 3 share IMAGE drawSeatCard (next (claude) task).

2026-06-27 23:00 UTC · Claude · files: public/lib.js, public/lib.test.mjs, public/app.js, public/styles.css, NIGHT_QUEUE.md, AGENT_LOG.md · commit: ede7627 · verification: `node --test public/lib.test.mjs` => 48 pass / 0 fail (44 prior + 4 new fitBox); `node --check public/app.js` PASS; `node --check public/lib.js` PASS; CSS braces balanced 154/154; EN/MS I18N parity (68 keys each, both diffs []); all data-i18n* markup keys resolve. Phase 3 share IMAGE drawSeatCard. (1) Added pure `fitBox(bbox, targetW, targetH, pad)` to lib.js — the share-card thumbnail transform: returns {scale, dx, dy} to fit a seat bbox into a target rect (aspect-preserving + centred), null on degenerate bbox / non-positive target / pad-eats-box / NaN. 4 new zero-dep tests: square-in-wide, padded non-zero-origin, a REAL P.001 bbox staying inside a 1080×760 padded region, and the degenerate/garbage→null table. (2) app.js: imported fitBox; new `drawSeatCard(seat)` paints an off-screen 1080×1350 canvas — accent header band (partyColor of the bloc, neutral when no result), `◧ MyPolitik` brand, the seat silhouette via `new Path2D(seat.d)` translated/scaled by fitBox + filled evenodd (try/caught so a Path2D-less engine still yields a text card), kicker·code, width-clamped seat name, state, YB name + rounded bloc pill + majority (or rep_ph when no GE15 row), and a `card_find <host>` CTA + src_ge15 footer. `shareCard()` awaits `document.fonts.ready` (guarded), `toBlob`→PNG `File`, prefers `navigator.canShare({files})`+`navigator.share` (AbortError = user-dismissed, silent), else an object-URL `<a download>` fallback; any throw → `card_fail` toast pointing to the 🔗 link. Re-entrancy-guarded (sharingCard). New `#share-card` button added to the existing `.seat-actions` (now flex row, .share-btn flex:1 1 140px so the two buttons share the row). 3 new I18N keys EN+BM (card_btn, card_fail, card_find). (3) styles.css: .seat-actions → flex/gap/wrap. [HVR] — fitBox is machine-verified; the Canvas rendering, share-sheet flow, fonts.ready timing and visual layout need a real browser → Danial's morning call (no browser/canvas in sandbox). · next: codex VERIFY card image (bbox→thumbnail transform unit-tested — done via fitBox, confirm; graceful Copy-link fallback when share unsupported), then Phase 4 regression / ♻️ critique-and-replenish if lists drain.
